---
phase: 05-multi-format-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/smdgf/export/__init__.py
  - src/smdgf/export/models.py
  - src/smdgf/export/qa.py
  - tests/unit/test_export_qa.py
autonomous: true
requirements: [EXPO-01, EXPO-03]
user_setup: []
must_haves:
  truths:
    - "The same approved canonical sample can render into QA and open-QA records."
    - "QA and open-QA export do not mutate canonical sample semantics."
    - "Export records preserve source sample ids and provenance metadata explicitly."
  artifacts:
    - path: "src/smdgf/export/models.py"
      provides: "Shared export record contracts and source-sample linkage"
      min_lines: 80
      contains: "class ExportRecord"
    - path: "src/smdgf/export/qa.py"
      provides: "Canonical-to-QA and canonical-to-open-QA rendering helpers"
      min_lines: 70
      contains: "def export_sample_to_qa"
    - path: "tests/unit/test_export_qa.py"
      provides: "QA and open-QA semantic-preservation tests"
      min_lines: 35
      contains: "def test_export_sample_to_open_qa_preserves_rationale"
  key_links:
    - from: "src/smdgf/export/qa.py"
      to: "src/smdgf/schemas/canonical.py"
      via: "export rendering consumes approved canonical samples directly"
      pattern: "CanonicalSample|CanonicalQuestion|CanonicalAnswer"
    - from: "src/smdgf/export/models.py"
      to: "src/smdgf/qc/models.py"
      via: "export records preserve QC-approved source identifiers and provenance"
      pattern: "QualityDecision|QualityCandidate"
---

<objective>
Implement shared export contracts plus canonical-to-QA and canonical-to-open-QA exporters.

Purpose: Phase 5 starts by rendering approved canonical samples into the simplest downstream benchmark formats while preserving source semantics.
Output: Shared export record models, QA/open-QA renderers, and unit tests proving semantic preservation.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-multi-format-export/05-RESEARCH.md
@src/smdgf/schemas/canonical.py
@src/smdgf/qc/models.py
@src/smdgf/qc/reports.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add export contracts and package exports</name>
  <files>src/smdgf/export/__init__.py, src/smdgf/export/models.py</files>
  <read_first>.planning/phases/05-multi-format-export/05-RESEARCH.md, .planning/REQUIREMENTS.md, src/smdgf/schemas/canonical.py, src/smdgf/qc/models.py</read_first>
  <action>Create `src/smdgf/export/__init__.py` and `src/smdgf/export/models.py`. Define strict Pydantic models for a shared `ExportRecord`, format-specific export payloads as needed, and source-linkage fields such as sample id, task id, format, split, and provenance metadata. Keep export models downstream from canonical and QC contracts rather than changing existing core schemas.</action>
  <verify>python3 - <<'PY'\nfrom pathlib import Path\nchecks = {\n  'src/smdgf/export/models.py': ['class ExportRecord', 'source_sample_id', 'format'],\n  'src/smdgf/export/__init__.py': ['ExportRecord'],\n}\nfor path, needles in checks.items():\n    text = Path(path).read_text()\n    if any(n not in text for n in needles):\n        raise SystemExit(1)\nPY</verify>
  <acceptance_criteria>
    - `src/smdgf/export/models.py` contains `class ExportRecord`
    - `src/smdgf/export/models.py` preserves source sample linkage fields
    - `src/smdgf/export/__init__.py` exports shared export symbols
  </acceptance_criteria>
  <done>Shared export contracts exist for downstream renderers.</done>
</task>

<task type="auto">
  <name>Task 2: Implement QA and open-QA renderers with unit tests</name>
  <files>src/smdgf/export/qa.py, tests/unit/test_export_qa.py</files>
  <read_first>.planning/phases/05-multi-format-export/05-VALIDATION.md, src/smdgf/export/models.py, src/smdgf/schemas/canonical.py, tests/conftest.py</read_first>
  <action>Create `src/smdgf/export/qa.py` with canonical-to-QA and canonical-to-open-QA rendering helpers that produce deterministic records from approved canonical samples. Add `tests/unit/test_export_qa.py` with tests named `test_export_sample_to_qa_preserves_source_ids` and `test_export_sample_to_open_qa_preserves_rationale`. Ensure open-QA can preserve richer answer payloads or rationale without mutating canonical inputs.</action>
  <verify>python3 -m pytest tests/unit/test_export_qa.py -q</verify>
  <acceptance_criteria>
    - `src/smdgf/export/qa.py` contains `def export_sample_to_qa`
    - `src/smdgf/export/qa.py` contains `def export_sample_to_open_qa`
    - `tests/unit/test_export_qa.py` contains `def test_export_sample_to_open_qa_preserves_rationale`
  </acceptance_criteria>
  <done>Approved canonical samples now render into QA and open-QA exports without semantic drift.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
  <boundary>
    <name>Approved canonical samples to export records</name>
    <description>QC-approved canonical samples cross into exporter code that renders benchmark-facing output formats.</description>
    <data_crossing>Canonical scene text, question text, answer payloads, source sample ids, QC metadata</data_crossing>
  </boundary>
</trust_boundaries>
<threat_register>
  <threat>
    <threat_id>T-5-01</threat_id>
    <category>Integrity</category>
    <component>QA export renderer</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Render QA records directly from approved canonical question and answer pairs so exporter logic cannot invent or mutate source semantics.</mitigation_plan>
    <files_to_check>src/smdgf/export/models.py, src/smdgf/export/qa.py, tests/unit/test_export_qa.py</files_to_check>
  </threat>
  <threat>
    <threat_id>T-5-02</threat_id>
    <category>Integrity</category>
    <component>open-QA export renderer</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Preserve richer answer payloads and rationale explicitly in open-QA export records instead of flattening them into lossy fields.</mitigation_plan>
    <files_to_check>src/smdgf/export/qa.py, tests/unit/test_export_qa.py</files_to_check>
  </threat>
</threat_register>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `python3 -m pytest tests/unit/test_export_qa.py -q` succeeds
- [ ] QA and open-QA records preserve source sample ids and approved canonical semantics
- [ ] Export models remain downstream from canonical and QC contracts
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- QA and open-QA export paths exist without canonical semantic drift
</success_criteria>

<output>
After completion, create `.planning/phases/05-multi-format-export/05-01-SUMMARY.md`
</output>
