---
phase: 05-multi-format-export
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/smdgf/export/models.py
  - src/smdgf/export/mcq.py
  - tests/unit/test_export_mcq.py
autonomous: true
requirements: [EXPO-02]
user_setup: []
must_haves:
  truths:
    - "The same approved canonical sample can render into an MCQ record."
    - "MCQ export exposes distractor strategy hooks without corrupting the correct answer semantics."
    - "Distractor generation remains deterministic under a fixed strategy and seed."
  artifacts:
    - path: "src/smdgf/export/mcq.py"
      provides: "Canonical-to-MCQ exporter and distractor strategy hooks"
      min_lines: 85
      contains: "class DistractorStrategy"
    - path: "tests/unit/test_export_mcq.py"
      provides: "MCQ and distractor validity tests"
      min_lines: 40
      contains: "def test_export_sample_to_mcq_keeps_unique_correct_answer"
  key_links:
    - from: "src/smdgf/export/mcq.py"
      to: "src/smdgf/export/models.py"
      via: "MCQ exporter emits shared export records plus MCQ-specific option payloads"
      pattern: "ExportRecord|ExportFormat"
    - from: "src/smdgf/export/mcq.py"
      to: "src/smdgf/schemas/task.py"
      via: "MCQ export respects task-level export support and answer semantics"
      pattern: "ExportFormat|AnswerMode"
---

<objective>
Implement canonical-to-MCQ export with deterministic distractor strategy hooks.

Purpose: MCQ export is the highest-risk format because weak distractors can distort benchmark validity.
Output: MCQ export contracts, distractor strategy interface, deterministic MCQ renderer, and unit tests for correct-answer uniqueness.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-multi-format-export/05-RESEARCH.md
@.planning/phases/05-multi-format-export/05-01-PLAN.md
@src/smdgf/export/models.py
@src/smdgf/schemas/task.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MCQ exporter and distractor strategy hooks</name>
  <files>src/smdgf/export/models.py, src/smdgf/export/mcq.py</files>
  <read_first>.planning/phases/05-multi-format-export/05-RESEARCH.md, src/smdgf/export/models.py, src/smdgf/schemas/task.py</read_first>
  <action>Extend `src/smdgf/export/models.py` as needed and create `src/smdgf/export/mcq.py` with a `DistractorStrategy` protocol or base class plus canonical-to-MCQ rendering helpers. The exporter should derive one correct option from the canonical answer, generate deterministic distractors through strategy hooks, and retain source sample linkage and strategy provenance.</action>
  <verify>python3 - <<'PY'\nfrom pathlib import Path\nchecks = {\n  'src/smdgf/export/mcq.py': ['class DistractorStrategy', 'def export_sample_to_mcq'],\n}\nfor path, needles in checks.items():\n    text = Path(path).read_text()\n    if any(n not in text for n in needles):\n        raise SystemExit(1)\nPY</verify>
  <acceptance_criteria>
    - `src/smdgf/export/mcq.py` contains `class DistractorStrategy`
    - `src/smdgf/export/mcq.py` contains `def export_sample_to_mcq`
    - MCQ export preserves one correct option from the canonical answer
  </acceptance_criteria>
  <done>MCQ export now has deterministic distractor hooks and a stable rendering surface.</done>
</task>

<task type="auto">
  <name>Task 2: Add MCQ validity and distractor tests</name>
  <files>tests/unit/test_export_mcq.py</files>
  <read_first>.planning/phases/05-multi-format-export/05-VALIDATION.md, src/smdgf/export/models.py, src/smdgf/export/mcq.py, tests/conftest.py</read_first>
  <action>Create `tests/unit/test_export_mcq.py` with tests named `test_export_sample_to_mcq_keeps_unique_correct_answer`, `test_distractor_strategy_can_supply_deterministic_options`, and `test_mcq_export_rejects_duplicate_correct_option`. Use stub distractor strategies only and avoid network or model calls.</action>
  <verify>python3 -m pytest tests/unit/test_export_mcq.py -q</verify>
  <acceptance_criteria>
    - `tests/unit/test_export_mcq.py` contains `def test_export_sample_to_mcq_keeps_unique_correct_answer`
    - `tests/unit/test_export_mcq.py` contains `def test_distractor_strategy_can_supply_deterministic_options`
  </acceptance_criteria>
  <done>MCQ export correctness and distractor behavior are now covered by deterministic tests.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
  <boundary>
    <name>Approved canonical samples to MCQ option generation</name>
    <description>Approved canonical answers cross into option-generation logic that can weaken or distort benchmark semantics if distractors are poorly formed.</description>
    <data_crossing>Canonical answer payloads, question text, distractor candidates, correct option metadata, strategy ids</data_crossing>
  </boundary>
</trust_boundaries>
<threat_register>
  <threat>
    <threat_id>T-5-03</threat_id>
    <category>Integrity</category>
    <component>MCQ option rendering</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Derive the correct option directly from canonical answers and validate that the final option list preserves one unambiguous correct answer.</mitigation_plan>
    <files_to_check>src/smdgf/export/mcq.py, tests/unit/test_export_mcq.py</files_to_check>
  </threat>
  <threat>
    <threat_id>T-5-04</threat_id>
    <category>Integrity</category>
    <component>distractor strategy hooks</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Keep distractor generation pluggable and deterministic so weak or duplicate distractors can be detected in tests and replaced without altering exporter core logic.</mitigation_plan>
    <files_to_check>src/smdgf/export/models.py, src/smdgf/export/mcq.py, tests/unit/test_export_mcq.py</files_to_check>
  </threat>
</threat_register>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `python3 -m pytest tests/unit/test_export_mcq.py -q` succeeds
- [ ] MCQ export preserves one unique correct answer
- [ ] Distractor strategies remain deterministic and replaceable
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- MCQ export preserves benchmark semantics while exposing distractor hooks
</success_criteria>

<output>
After completion, create `.planning/phases/05-multi-format-export/05-02-SUMMARY.md`
</output>
