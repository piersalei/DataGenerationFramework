---
phase: 05-multi-format-export
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - src/smdgf/export/models.py
  - src/smdgf/export/manifest.py
  - tests/unit/test_export_manifest.py
autonomous: true
requirements: [EXPO-04]
user_setup: []
must_haves:
  truths:
    - "Export runs emit manifests describing rendered formats, splits, configs, and artifact locations."
    - "Export artifacts are grouped in deterministic run layouts."
    - "Export manifests remain suitable for downstream reproducibility work in Phase 6."
  artifacts:
    - path: "src/smdgf/export/manifest.py"
      provides: "Export-run manifest and packaged layout helpers"
      min_lines: 85
      contains: "class ExportRunManifest"
    - path: "tests/unit/test_export_manifest.py"
      provides: "Manifest and artifact-layout tests"
      min_lines: 40
      contains: "def test_export_manifest_records_formats_splits_and_paths"
  key_links:
    - from: "src/smdgf/export/manifest.py"
      to: "src/smdgf/export/models.py"
      via: "manifest helpers persist structured export record and split metadata"
      pattern: "ExportRecord|ExportSplit"
    - from: "src/smdgf/export/manifest.py"
      to: "src/smdgf/qc/reports.py"
      via: "export runs retain references to upstream QC artifacts and approved sample sources"
      pattern: "QualityControlReport|run_id"
---

<objective>
Implement export manifests, split inventory, and packaged artifact layouts.

Purpose: Export is only complete when rendered datasets are accompanied by structured manifests that later reproducibility work can trust.
Output: Export-run manifest contracts, deterministic artifact-layout helpers, and unit tests for paths, splits, and config snapshots.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-multi-format-export/05-RESEARCH.md
@.planning/phases/05-multi-format-export/05-02-PLAN.md
@src/smdgf/export/models.py
@src/smdgf/qc/reports.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add export-run manifest and artifact-layout helpers</name>
  <files>src/smdgf/export/models.py, src/smdgf/export/manifest.py</files>
  <read_first>.planning/phases/05-multi-format-export/05-RESEARCH.md, src/smdgf/export/models.py, src/smdgf/qc/reports.py</read_first>
  <action>Extend `src/smdgf/export/models.py` as needed and create `src/smdgf/export/manifest.py` with `ExportRunManifest`, split inventory helpers, config snapshot fields, and deterministic output-path helpers for one export run directory. Ensure manifests can be serialized and later replayed without requiring hidden state.</action>
  <verify>python3 - <<'PY'\nfrom pathlib import Path\nchecks = {\n  'src/smdgf/export/manifest.py': ['class ExportRunManifest', 'def build_export_layout', 'def write_export_manifest'],\n}\nfor path, needles in checks.items():\n    text = Path(path).read_text()\n    if any(n not in text for n in needles):\n        raise SystemExit(1)\nPY</verify>
  <acceptance_criteria>
    - `src/smdgf/export/manifest.py` contains `class ExportRunManifest`
    - `src/smdgf/export/manifest.py` contains `def build_export_layout`
    - `src/smdgf/export/manifest.py` contains `def write_export_manifest`
  </acceptance_criteria>
  <done>Export runs can now persist deterministic manifests and output layouts.</done>
</task>

<task type="auto">
  <name>Task 2: Add manifest and artifact-layout tests</name>
  <files>tests/unit/test_export_manifest.py</files>
  <read_first>.planning/phases/05-multi-format-export/05-VALIDATION.md, src/smdgf/export/models.py, src/smdgf/export/manifest.py, tests/conftest.py</read_first>
  <action>Create `tests/unit/test_export_manifest.py` with tests named `test_export_manifest_records_formats_splits_and_paths`, `test_export_layout_groups_artifacts_by_run_id`, and `test_manifest_round_trip_preserves_config_snapshot`. Use temporary directories and deterministic fixtures only.</action>
  <verify>python3 -m pytest tests/unit/test_export_manifest.py -q</verify>
  <acceptance_criteria>
    - `tests/unit/test_export_manifest.py` contains `def test_export_manifest_records_formats_splits_and_paths`
    - `tests/unit/test_export_manifest.py` contains `def test_manifest_round_trip_preserves_config_snapshot`
  </acceptance_criteria>
  <done>Export manifests and artifact layouts are now covered by deterministic tests.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
  <boundary>
    <name>Rendered export records to persisted export artifacts</name>
    <description>Approved rendered datasets cross into filesystem layout and manifest persistence that downstream phases rely on for reproducibility and packaging.</description>
    <data_crossing>Export records, split names, run ids, config snapshots, artifact paths, QC references</data_crossing>
  </boundary>
</trust_boundaries>
<threat_register>
  <threat>
    <threat_id>T-5-05</threat_id>
    <category>Repudiation</category>
    <component>export manifests</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Persist formats, splits, config snapshots, and artifact paths as structured manifest fields so export runs can be audited and replayed later.</mitigation_plan>
    <files_to_check>src/smdgf/export/models.py, src/smdgf/export/manifest.py, tests/unit/test_export_manifest.py</files_to_check>
  </threat>
  <threat>
    <threat_id>T-5-06</threat_id>
    <category>Repudiation</category>
    <component>artifact layout grouping</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Use deterministic run-level layout helpers so exported artifacts are organized predictably and remain compatible with later reproducibility tooling.</mitigation_plan>
    <files_to_check>src/smdgf/export/manifest.py, tests/unit/test_export_manifest.py</files_to_check>
  </threat>
</threat_register>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `python3 -m pytest tests/unit/test_export_manifest.py -q` succeeds
- [ ] Export manifests persist formats, splits, config snapshots, and artifact paths explicitly
- [ ] Export layouts are deterministic and grouped by run id
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Export outputs are packaged with deterministic manifests ready for Phase 6
</success_criteria>

<output>
After completion, create `.planning/phases/05-multi-format-export/05-03-SUMMARY.md`
</output>
