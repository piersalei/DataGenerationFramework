---
phase: 02-scenario-and-sampling-engine
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/smdgf/samplers/__init__.py
  - src/smdgf/samplers/context.py
  - src/smdgf/samplers/scenario.py
  - tests/unit/test_sampling_engine.py
autonomous: true
requirements: [SCEN-03, SCEN-04]
user_setup: []
must_haves:
  truths:
    - "Researchers can sample scenario instances deterministically from a seed."
    - "Sampled scenarios include explicit role instances, relations, and latent-state assignments."
    - "The same template and seed produce identical sampled outputs across runs."
  artifacts:
    - path: "src/smdgf/samplers/context.py"
      provides: "Seeded sampling context and deterministic RNG helpers"
      min_lines: 25
      contains: "class SamplingContext"
    - path: "src/smdgf/samplers/scenario.py"
      provides: "Scenario sampler that instantiates templates into ScenarioSample objects"
      min_lines: 50
      contains: "def sample_scenario"
    - path: "tests/unit/test_sampling_engine.py"
      provides: "Deterministic sampling and latent-state assignment tests"
      min_lines: 25
      contains: "def test_sampling_is_deterministic_for_same_seed"
  key_links:
    - from: "src/smdgf/samplers/scenario.py"
      to: "src/smdgf/schemas/scene.py"
      via: "scenario sampler instantiates SceneTemplate into ScenarioSample"
      pattern: "SceneTemplate|ScenarioSample"
    - from: "src/smdgf/samplers/context.py"
      to: "src/smdgf/samplers/scenario.py"
      via: "deterministic RNG consumed by scenario sampling"
      pattern: "SamplingContext"
---

<objective>
Implement deterministic seeded sampling for scene templates, role instances, relations, and latent-state assignments.

Purpose: Later generation and QC phases need scenario instances that are reproducible, inspectable, and independent from provider execution.
Output: Sampler package with seeded context, scenario sampling logic, and deterministic sampling tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-scenario-and-sampling-engine/02-RESEARCH.md
@.planning/phases/02-scenario-and-sampling-engine/02-01-PLAN.md
@src/smdgf/schemas/scene.py
@src/smdgf/schemas/canonical.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement seeded sampling context and deterministic helper surface</name>
  <files>src/smdgf/samplers/__init__.py, src/smdgf/samplers/context.py</files>
  <read_first>.planning/phases/02-scenario-and-sampling-engine/02-RESEARCH.md, .planning/REQUIREMENTS.md, src/smdgf/schemas/scene.py, src/smdgf/schemas/canonical.py</read_first>
  <action>Create `src/smdgf/samplers/__init__.py` and `src/smdgf/samplers/context.py`. Implement `SamplingContext` with exact fields `seed`, `rng`, and `metadata`, plus deterministic helper methods `choose`, `shuffle_copy`, and `child`. Use a single `random.Random(seed)` instance internally. Ensure `child` derives deterministic sub-seeds from the parent seed plus a stable label string rather than relying on Python hash randomization.</action>
  <verify>python3 - <<'PY'\nfrom pathlib import Path\nchecks = {\n  'src/smdgf/samplers/context.py': ['class SamplingContext', 'def choose', 'def shuffle_copy', 'def child'],\n  'src/smdgf/samplers/__init__.py': ['SamplingContext'],\n}\nfor path, needles in checks.items():\n    text = Path(path).read_text()\n    if any(n not in text for n in needles):\n        raise SystemExit(1)\nPY</verify>
  <acceptance_criteria>
    - `src/smdgf/samplers/context.py` contains `class SamplingContext`
    - `src/smdgf/samplers/context.py` contains `def choose`
    - `src/smdgf/samplers/context.py` contains `def child`
  </acceptance_criteria>
  <done>Seeded sampling context exists and can be reused consistently across scenario samplers.</done>
</task>

<task type="auto">
  <name>Task 2: Implement scenario sampler and deterministic sampling tests</name>
  <files>src/smdgf/samplers/scenario.py, tests/unit/test_sampling_engine.py</files>
  <read_first>.planning/phases/02-scenario-and-sampling-engine/02-RESEARCH.md, src/smdgf/samplers/context.py, src/smdgf/schemas/scene.py, tests/conftest.py</read_first>
  <action>Create `src/smdgf/samplers/scenario.py` with a function `sample_scenario(template: SceneTemplate, context: SamplingContext) -> ScenarioSample`. Materialize sampled slots in sorted key order, create sampled roles, carry explicit relation edges, and assign latent states using deterministic picks from `allowed_values`. Add `tests/unit/test_sampling_engine.py` with tests named `test_sampling_is_deterministic_for_same_seed`, `test_sampling_varies_for_different_seeds`, and `test_sampling_preserves_latent_state_assignments`. Make assertions against concrete sampled fields rather than string-only placeholders.</action>
  <verify>python3 -m pytest tests/unit/test_sampling_engine.py -q</verify>
  <acceptance_criteria>
    - `src/smdgf/samplers/scenario.py` contains `def sample_scenario`
    - `tests/unit/test_sampling_engine.py` contains `def test_sampling_is_deterministic_for_same_seed`
    - `tests/unit/test_sampling_engine.py` contains `def test_sampling_preserves_latent_state_assignments`
  </acceptance_criteria>
  <done>Scenario sampling is deterministic for the same seed and explicit about latent-state assignments.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
  <boundary>
    <name>Seeded sampling context to scenario instantiation</name>
    <description>Deterministic RNG decisions control slot filling, role materialization, and latent-state assignment.</description>
    <data_crossing>Seed values, derived sub-seeds, sampled slots, latent-state assignments</data_crossing>
  </boundary>
</trust_boundaries>
<threat_register>
  <threat>
    <threat_id>T-2-03</threat_id>
    <category>Tampering</category>
    <component>sampling context</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Centralize all randomness in `SamplingContext` and derive child seeds deterministically from stable labels.</mitigation_plan>
    <files_to_check>src/smdgf/samplers/context.py, tests/unit/test_sampling_engine.py</files_to_check>
  </threat>
  <threat>
    <threat_id>T-2-04</threat_id>
    <category>Repudiation</category>
    <component>scenario sampler</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Emit sampled slots and latent-state assignments explicitly inside `ScenarioSample` so reruns can be compared directly.</mitigation_plan>
    <files_to_check>src/smdgf/samplers/scenario.py, tests/unit/test_sampling_engine.py</files_to_check>
  </threat>
</threat_register>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `python3 -m pytest tests/unit/test_sampling_engine.py -q` succeeds
- [ ] `sample_scenario` returns deterministic outputs for the same seed
- [ ] Scenario samples expose explicit latent-state assignments and relations
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Deterministic scenario sampling is ready for preview and later provider integration
</success_criteria>

<output>
After completion, create `.planning/phases/02-scenario-and-sampling-engine/02-02-SUMMARY.md`
</output>
