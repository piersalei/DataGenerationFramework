---
phase: 02-scenario-and-sampling-engine
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/smdgf/samplers/scenario.py
  - src/smdgf/cli/main.py
  - src/smdgf/cli/sampling.py
  - tests/fixtures/scene_template_valid.yaml
  - tests/unit/test_sampling_preview.py
autonomous: true
requirements: [SCEN-01, SCEN-04]
user_setup: []
must_haves:
  truths:
    - "Researchers can preview sampled scenarios without invoking an LLM provider."
    - "Preview output is reproducible for the same template and seed."
    - "Scene preview utilities make sampled roles, slots, relations, and latent states inspectable."
  artifacts:
    - path: "src/smdgf/cli/sampling.py"
      provides: "CLI preview command for scene sampling"
      min_lines: 30
      contains: "def preview"
    - path: "tests/fixtures/scene_template_valid.yaml"
      provides: "Valid scene template fixture for preview tests"
      min_lines: 20
      contains: "scene_blueprint:"
    - path: "tests/unit/test_sampling_preview.py"
      provides: "Preview and reproducibility tests"
      min_lines: 25
      contains: "def test_preview_is_reproducible_for_same_seed"
  key_links:
    - from: "src/smdgf/cli/sampling.py"
      to: "src/smdgf/samplers/scenario.py"
      via: "preview command invokes deterministic scenario sampling"
      pattern: "sample_scenario"
    - from: "tests/unit/test_sampling_preview.py"
      to: "tests/fixtures/scene_template_valid.yaml"
      via: "fixture-backed preview validation"
      pattern: "scene_template_valid\\.yaml"
---

<objective>
Add preview utilities and tests that let researchers inspect deterministic sampled scenarios before provider-based generation begins.

Purpose: Phase 2 should end with an inspectable scenario engine so later generation logic builds on concrete sampled worlds instead of blind internal state.
Output: Preview CLI command, reusable fixture template, and preview-focused reproducibility tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-scenario-and-sampling-engine/02-RESEARCH.md
@.planning/phases/02-scenario-and-sampling-engine/02-02-PLAN.md
@src/smdgf/samplers/context.py
@src/smdgf/samplers/scenario.py
@src/smdgf/cli/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sampling preview CLI and fixture-backed loading path</name>
  <files>src/smdgf/cli/main.py, src/smdgf/cli/sampling.py, tests/fixtures/scene_template_valid.yaml</files>
  <read_first>.planning/phases/02-scenario-and-sampling-engine/02-RESEARCH.md, src/smdgf/samplers/scenario.py, src/smdgf/schemas/scene.py, src/smdgf/cli/main.py</read_first>
  <action>Create `src/smdgf/cli/sampling.py` and register it from `src/smdgf/cli/main.py` as a `sampling` sub-app. Implement a `preview` command that accepts a scene-template file path plus `--seed`, loads YAML or JSON into `SceneTemplate`, runs `sample_scenario`, and prints a deterministic JSON representation that includes `template_id`, sampled slots, roles, relations, and latent-state assignments. Create `tests/fixtures/scene_template_valid.yaml` with a realistic template that uses multiple roles, at least one relation edge, and at least one latent-state spec.</action>
  <verify>python3 - <<'PY'\nfrom pathlib import Path\nchecks = {\n  'src/smdgf/cli/sampling.py': ['def preview', 'sample_scenario'],\n  'src/smdgf/cli/main.py': ['sampling'],\n  'tests/fixtures/scene_template_valid.yaml': ['scene_blueprint:', 'relations:', 'latent_state_specs:'],\n}\nfor path, needles in checks.items():\n    text = Path(path).read_text()\n    if any(n not in text for n in needles):\n        raise SystemExit(1)\nPY</verify>
  <acceptance_criteria>
    - `src/smdgf/cli/sampling.py` contains `def preview`
    - `src/smdgf/cli/sampling.py` contains `sample_scenario`
    - `tests/fixtures/scene_template_valid.yaml` contains `latent_state_specs:`
  </acceptance_criteria>
  <done>Researchers can invoke a preview path over a valid scene template fixture.</done>
</task>

<task type="auto">
  <name>Task 2: Add preview reproducibility tests and explicit output checks</name>
  <files>tests/unit/test_sampling_preview.py</files>
  <read_first>.planning/phases/02-scenario-and-sampling-engine/02-VALIDATION.md, src/smdgf/cli/sampling.py, tests/fixtures/scene_template_valid.yaml, tests/conftest.py</read_first>
  <action>Create `tests/unit/test_sampling_preview.py` with tests named `test_preview_is_reproducible_for_same_seed`, `test_preview_changes_for_different_seeds`, and `test_preview_output_exposes_roles_relations_and_latent_states`. Use Typer `CliRunner` or direct function invocation to verify preview output is deterministic for the same seed, differs across at least one sampled field for different seeds, and contains explicit keys for roles, relations, and latent-state assignments.</action>
  <verify>python3 -m pytest tests/unit/test_sampling_preview.py -q</verify>
  <acceptance_criteria>
    - `tests/unit/test_sampling_preview.py` contains `def test_preview_is_reproducible_for_same_seed`
    - `tests/unit/test_sampling_preview.py` contains `def test_preview_changes_for_different_seeds`
    - `tests/unit/test_sampling_preview.py` contains `def test_preview_output_exposes_roles_relations_and_latent_states`
  </acceptance_criteria>
  <done>Preview tests prove deterministic and inspectable scenario sampling output.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
  <boundary>
    <name>Template fixture loading to preview output</name>
    <description>User-provided scene-template files cross into preview rendering and deterministic output generation.</description>
    <data_crossing>Template payloads, seed values, preview JSON output</data_crossing>
  </boundary>
</trust_boundaries>
<threat_register>
  <threat>
    <threat_id>T-2-05</threat_id>
    <category>Tampering</category>
    <component>preview command</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Only parse YAML/JSON into strict `SceneTemplate` models and never execute template content.</mitigation_plan>
    <files_to_check>src/smdgf/cli/sampling.py, tests/unit/test_sampling_preview.py</files_to_check>
  </threat>
  <threat>
    <threat_id>T-2-06</threat_id>
    <category>Repudiation</category>
    <component>preview output</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Emit deterministic JSON with explicit sampled roles, relations, and latent-state assignments so outputs can be diffed across seeds.</mitigation_plan>
    <files_to_check>src/smdgf/cli/sampling.py, tests/unit/test_sampling_preview.py</files_to_check>
  </threat>
</threat_register>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `python3 -m pytest tests/unit/test_sampling_preview.py -q` succeeds
- [ ] Preview CLI returns deterministic output for the same seed
- [ ] Preview output exposes roles, relations, and latent-state assignments explicitly
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 2 ends with an inspectable deterministic scenario preview path
</success_criteria>

<output>
After completion, create `.planning/phases/02-scenario-and-sampling-engine/02-03-SUMMARY.md`
</output>
