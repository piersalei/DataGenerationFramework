---
phase: 02-scenario-and-sampling-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/smdgf/schemas/scene.py
  - src/smdgf/schemas/__init__.py
  - tests/unit/test_scene_models.py
autonomous: true
requirements: [SCEN-01, SCEN-02, SCEN-03]
user_setup: []
must_haves:
  truths:
    - "Researchers can define scene templates with typed slots and explicit constraints."
    - "Scene templates can express multiple roles and explicit role relations."
    - "Latent mental states are modeled as structured fields instead of prompt-only prose."
  artifacts:
    - path: "src/smdgf/schemas/scene.py"
      provides: "SceneTemplate, RoleSpec, RelationSpec, LatentStateSpec, and ScenarioSample models"
      min_lines: 60
      contains: "class SceneTemplate"
    - path: "tests/unit/test_scene_models.py"
      provides: "Contract tests for scene templates, roles, relations, and latent-state specs"
      min_lines: 25
      contains: "def test_scene_template_requires_typed_slots"
  key_links:
    - from: "src/smdgf/schemas/scene.py"
      to: "src/smdgf/schemas/task.py"
      via: "scene templates reference task ids and contract-layer types"
      pattern: "task_id"
    - from: "tests/unit/test_scene_models.py"
      to: "src/smdgf/schemas/scene.py"
      via: "unit coverage for scene contracts"
      pattern: "SceneTemplate|RoleSpec|LatentStateSpec"
---

<objective>
Implement the schema contracts for scene templates, role relations, latent-state specifications, and sampled scenario containers.

Purpose: Phase 2 must establish explicit scenario and latent-state structure before any deterministic sampling logic can be trusted.
Output: `scene.py` contract models plus focused scene-model tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-scenario-and-sampling-engine/02-RESEARCH.md
@.planning/phases/01-canonical-contracts/01-02-SUMMARY.md
@src/smdgf/schemas/task.py
@src/smdgf/schemas/canonical.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement scene, role, relation, and latent-state contracts</name>
  <files>src/smdgf/schemas/scene.py, src/smdgf/schemas/__init__.py</files>
  <read_first>.planning/phases/02-scenario-and-sampling-engine/02-RESEARCH.md, .planning/REQUIREMENTS.md, .planning/phases/01-canonical-contracts/01-02-SUMMARY.md, src/smdgf/schemas/task.py, src/smdgf/schemas/canonical.py, src/smdgf/schemas/__init__.py</read_first>
  <action>Create `src/smdgf/schemas/scene.py` with strict Pydantic models for `SlotSpec`, `RoleSpec`, `RelationSpec`, `LatentStateSpec`, `SceneConstraint`, `SceneTemplate`, `SampledRole`, `SampledRelation`, `LatentStateAssignment`, and `ScenarioSample`. Ensure `SceneTemplate` has exact fields `template_id`, `task_id`, `scene_blueprint`, `slot_specs`, `roles`, `relations`, `latent_state_specs`, and `constraints`. Ensure role relations are explicit edges using `source_role`, `relation_type`, and `target_role`. Ensure latent-state specs capture `state_id`, `owner_role`, `state_type`, `allowed_values`, `sampling_strategy`, and `required`. Export the new scene contract symbols from `src/smdgf/schemas/__init__.py`.</action>
  <verify>python3 - <<'PY'\nfrom pathlib import Path\nchecks = {\n  'src/smdgf/schemas/scene.py': ['class SceneTemplate', 'class RoleSpec', 'class RelationSpec', 'class LatentStateSpec', 'class ScenarioSample'],\n  'src/smdgf/schemas/__init__.py': ['SceneTemplate', 'RoleSpec', 'ScenarioSample'],\n}\nfor path, needles in checks.items():\n    text = Path(path).read_text()\n    if any(n not in text for n in needles):\n        raise SystemExit(1)\nPY</verify>
  <acceptance_criteria>
    - `src/smdgf/schemas/scene.py` contains `class SceneTemplate`
    - `src/smdgf/schemas/scene.py` contains `class RoleSpec`
    - `src/smdgf/schemas/scene.py` contains `class LatentStateSpec`
    - `src/smdgf/schemas/scene.py` contains `class ScenarioSample`
  </acceptance_criteria>
  <done>Scene contract layer exists with explicit role, relation, and latent-state structures.</done>
</task>

<task type="auto">
  <name>Task 2: Add contract tests for typed slots, relations, and latent-state structure</name>
  <files>tests/unit/test_scene_models.py</files>
  <read_first>.planning/phases/02-scenario-and-sampling-engine/02-VALIDATION.md, src/smdgf/schemas/scene.py, src/smdgf/schemas/task.py, tests/conftest.py</read_first>
  <action>Create `tests/unit/test_scene_models.py` with tests named `test_scene_template_requires_typed_slots`, `test_scene_template_supports_role_relations`, and `test_latent_state_spec_is_structured`. Verify that slot specs are typed, relation edges preserve source and target role ids, and latent-state specs capture owner role and sampling strategy as explicit fields. Use real instantiated models rather than string-only smoke assertions.</action>
  <verify>python3 -m pytest tests/unit/test_scene_models.py -q</verify>
  <acceptance_criteria>
    - `tests/unit/test_scene_models.py` contains `def test_scene_template_requires_typed_slots`
    - `tests/unit/test_scene_models.py` contains `def test_scene_template_supports_role_relations`
    - `tests/unit/test_scene_models.py` contains `def test_latent_state_spec_is_structured`
  </acceptance_criteria>
  <done>Scene-model tests verify typed slots, relation edges, and structured latent states.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
  <boundary>
    <name>Scene template definitions to sampling engine</name>
    <description>Research-authored templates and latent-state specs cross into the deterministic scenario layer.</description>
    <data_crossing>Slot specs, role graph metadata, relation edges, latent-state requirements</data_crossing>
  </boundary>
</trust_boundaries>
<threat_register>
  <threat>
    <threat_id>T-2-01</threat_id>
    <category>Tampering</category>
    <component>scene contracts</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Use strict Pydantic contracts so malformed scene templates, slot specs, or relation edges fail validation immediately.</mitigation_plan>
    <files_to_check>src/smdgf/schemas/scene.py, tests/unit/test_scene_models.py</files_to_check>
  </threat>
  <threat>
    <threat_id>T-2-02</threat_id>
    <category>Information Disclosure</category>
    <component>latent-state modeling</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Store latent states as structured assignments instead of hidden prompt prose so later phases can audit and control them explicitly.</mitigation_plan>
    <files_to_check>src/smdgf/schemas/scene.py, tests/unit/test_scene_models.py</files_to_check>
  </threat>
</threat_register>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `python3 -m pytest tests/unit/test_scene_models.py -q` succeeds
- [ ] Scene templates support typed slots, explicit role relations, and structured latent-state specs
- [ ] `src/smdgf/schemas/__init__.py` exports the new scene contracts
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Scene contracts are ready for deterministic sampling in the next wave
</success_criteria>

<output>
After completion, create `.planning/phases/02-scenario-and-sampling-engine/02-01-SUMMARY.md`
</output>
