---
phase: 04-quality-control-pipeline
plan: 04
type: execute
wave: 4
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - src/smdgf/qc/models.py
  - src/smdgf/qc/reports.py
  - tests/unit/test_qc_reports.py
autonomous: true
requirements: [QUAL-05, QUAL-06]
user_setup: []
must_haves:
  truths:
    - "Rejected and flagged samples produce structured QC artifacts that can be audited after a run."
    - "QC reports summarize acceptance metrics, rejection breakdowns, and duplicate clusters."
    - "Flagged samples can flow through a human review and arbitration path with keep, revise, and discard outcomes."
  artifacts:
    - path: "src/smdgf/qc/reports.py"
      provides: "QC report, rejection manifest, review queue, and metrics helpers"
      min_lines: 90
      contains: "class QualityControlReport"
    - path: "tests/unit/test_qc_reports.py"
      provides: "QC report and review queue tests"
      min_lines: 40
      contains: "def test_review_queue_preserves_keep_revise_discard_outcomes"
  key_links:
    - from: "src/smdgf/qc/reports.py"
      to: "src/smdgf/qc/models.py"
      via: "reports aggregate QC findings, review states, and final dispositions"
      pattern: "QualityDecision|ReviewDisposition|QualityFinding"
    - from: "src/smdgf/qc/reports.py"
      to: "src/smdgf/generation/models.py"
      via: "report artifacts remain linked to generation run ids and provenance"
      pattern: "GenerationRunManifest|run_id"
---

<objective>
Emit QC audit artifacts, review queues, and acceptance metrics.

Purpose: Phase 4 is only complete if QC decisions can be inspected, summarized, and routed into human arbitration when automatic checks are inconclusive.
Output: QC report models and serialization helpers, rejection manifests, review queue support, acceptance metrics, and unit tests for audit artifacts.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-quality-control-pipeline/04-RESEARCH.md
@.planning/phases/04-quality-control-pipeline/04-02-PLAN.md
@.planning/phases/04-quality-control-pipeline/04-03-PLAN.md
@src/smdgf/qc/models.py
@src/smdgf/generation/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add QC report, rejection manifest, and review queue models</name>
  <files>src/smdgf/qc/models.py, src/smdgf/qc/reports.py</files>
  <read_first>.planning/phases/04-quality-control-pipeline/04-RESEARCH.md, src/smdgf/qc/models.py, src/smdgf/generation/models.py</read_first>
  <action>Extend `src/smdgf/qc/models.py` as needed and create `src/smdgf/qc/reports.py` with report models and helpers for `QualityControlReport`, rejection manifests, review queue entries, arbitration records, and acceptance metrics summaries. Ensure artifacts can be serialized deterministically and linked back to generation run ids and candidate ids.</action>
  <verify>python3 - <<'PY'\nfrom pathlib import Path\nchecks = {\n  'src/smdgf/qc/reports.py': ['class QualityControlReport', 'class ReviewQueueEntry', 'def summarize_metrics'],\n}\nfor path, needles in checks.items():\n    text = Path(path).read_text()\n    if any(n not in text for n in needles):\n        raise SystemExit(1)\nPY</verify>
  <acceptance_criteria>
    - `src/smdgf/qc/reports.py` contains `class QualityControlReport`
    - `src/smdgf/qc/reports.py` contains `class ReviewQueueEntry`
    - `src/smdgf/qc/reports.py` contains `def summarize_metrics`
  </acceptance_criteria>
  <done>QC outcomes can now be persisted as structured audit artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for reports, rejection reasons, and review arbitration</name>
  <files>tests/unit/test_qc_reports.py</files>
  <read_first>.planning/phases/04-quality-control-pipeline/04-VALIDATION.md, src/smdgf/qc/models.py, src/smdgf/qc/reports.py, tests/conftest.py</read_first>
  <action>Create `tests/unit/test_qc_reports.py` with tests named `test_qc_report_summarizes_acceptance_and_rejection_metrics`, `test_rejection_manifest_preserves_rule_reasons_and_candidate_ids`, and `test_review_queue_preserves_keep_revise_discard_outcomes`. Use deterministic QC result fixtures and file round-trip checks where useful.</action>
  <verify>python3 -m pytest tests/unit/test_qc_reports.py -q</verify>
  <acceptance_criteria>
    - `tests/unit/test_qc_reports.py` contains `def test_qc_report_summarizes_acceptance_and_rejection_metrics`
    - `tests/unit/test_qc_reports.py` contains `def test_review_queue_preserves_keep_revise_discard_outcomes`
  </acceptance_criteria>
  <done>QC artifacts are now auditable, reviewable, and measurable after a run.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
  <boundary>
    <name>QC decisions to persisted audit artifacts</name>
    <description>Automatic QC outcomes and human review dispositions cross into persisted reports that future benchmark runs and audits depend on.</description>
    <data_crossing>Candidate ids, findings, rejection reasons, metrics, review notes, arbitration outcomes, run ids</data_crossing>
  </boundary>
</trust_boundaries>
<threat_register>
  <threat>
    <threat_id>T-4-07</threat_id>
    <category>Repudiation</category>
    <component>QC audit artifacts</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Persist rejection manifests and acceptance metrics as structured artifacts keyed by candidate and run ids so researchers can audit decisions after execution.</mitigation_plan>
    <files_to_check>src/smdgf/qc/reports.py, tests/unit/test_qc_reports.py</files_to_check>
  </threat>
  <threat>
    <threat_id>T-4-08</threat_id>
    <category>Repudiation</category>
    <component>human review and arbitration</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Represent review queue entries and final keep, revise, discard outcomes explicitly so human arbitration is inspectable instead of implicit.</mitigation_plan>
    <files_to_check>src/smdgf/qc/models.py, src/smdgf/qc/reports.py, tests/unit/test_qc_reports.py</files_to_check>
  </threat>
</threat_register>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `python3 -m pytest tests/unit/test_qc_reports.py -q` succeeds
- [ ] Rejection reasons and acceptance metrics serialize into auditable artifacts
- [ ] Review queue entries preserve explicit keep, revise, and discard outcomes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 4 produces audit-ready QC artifacts and review routing primitives
</success_criteria>

<output>
After completion, create `.planning/phases/04-quality-control-pipeline/04-04-SUMMARY.md`
</output>
