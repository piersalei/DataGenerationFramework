---
phase: 04-quality-control-pipeline
plan: 03
type: execute
wave: 3
depends_on: ["04-01"]
files_modified:
  - src/smdgf/qc/models.py
  - src/smdgf/qc/dedup.py
  - tests/unit/test_qc_dedup.py
autonomous: true
requirements: [QUAL-04]
user_setup: []
must_haves:
  truths:
    - "Exact duplicates among accepted samples are detected with stable fingerprints."
    - "Near-duplicate detection can identify lexically similar accepted samples deterministically."
    - "Duplicate handling preserves cluster evidence for later audit and review."
  artifacts:
    - path: "src/smdgf/qc/dedup.py"
      provides: "Exact and near-duplicate detection helpers with stable fingerprinting"
      min_lines: 80
      contains: "def fingerprint_candidate"
    - path: "tests/unit/test_qc_dedup.py"
      provides: "Duplicate and near-duplicate tests"
      min_lines: 35
      contains: "def test_near_duplicate_detector_groups_similar_questions"
  key_links:
    - from: "src/smdgf/qc/dedup.py"
      to: "src/smdgf/qc/models.py"
      via: "duplicate detection consumes QC candidate contracts and emits cluster metadata"
      pattern: "QualityCandidate|DuplicateCluster"
    - from: "src/smdgf/qc/dedup.py"
      to: "src/smdgf/schemas/canonical.py"
      via: "exact fingerprints are derived from canonical semantics rather than exporter-specific records"
      pattern: "CanonicalSample"
---

<objective>
Implement exact and near-duplicate detection over accepted canonical candidates.

Purpose: High-volume data generation will otherwise create repeated or trivially varied items that weaken benchmark coverage.
Output: Stable fingerprinting, near-duplicate grouping helpers, and unit tests proving duplicate handling is deterministic.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-quality-control-pipeline/04-RESEARCH.md
@.planning/phases/04-quality-control-pipeline/04-01-PLAN.md
@src/smdgf/qc/models.py
@src/smdgf/schemas/canonical.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add exact and near-duplicate detection helpers</name>
  <files>src/smdgf/qc/models.py, src/smdgf/qc/dedup.py</files>
  <read_first>.planning/phases/04-quality-control-pipeline/04-RESEARCH.md, src/smdgf/qc/models.py, src/smdgf/schemas/canonical.py</read_first>
  <action>Extend `src/smdgf/qc/models.py` as needed and create `src/smdgf/qc/dedup.py` with stable exact fingerprinting plus deterministic near-duplicate grouping based on normalized text or token overlap. Include any data structures needed to represent duplicate clusters, duplicate reasons, and recommended actions. Keep the implementation lightweight and reproducible under Python 3.9.</action>
  <verify>python3 - <<'PY'\nfrom pathlib import Path\nchecks = {\n  'src/smdgf/qc/dedup.py': ['def fingerprint_candidate', 'def detect_duplicates', 'def detect_near_duplicates'],\n}\nfor path, needles in checks.items():\n    text = Path(path).read_text()\n    if any(n not in text for n in needles):\n        raise SystemExit(1)\nPY</verify>
  <acceptance_criteria>
    - `src/smdgf/qc/dedup.py` contains `def fingerprint_candidate`
    - `src/smdgf/qc/dedup.py` contains `def detect_duplicates`
    - `src/smdgf/qc/dedup.py` contains `def detect_near_duplicates`
  </acceptance_criteria>
  <done>Accepted candidates can now be clustered by exact or near-duplicate similarity.</done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for duplicate clustering</name>
  <files>tests/unit/test_qc_dedup.py</files>
  <read_first>.planning/phases/04-quality-control-pipeline/04-VALIDATION.md, src/smdgf/qc/models.py, src/smdgf/qc/dedup.py, tests/conftest.py</read_first>
  <action>Create `tests/unit/test_qc_dedup.py` with tests named `test_exact_duplicate_detector_flags_same_canonical_signature`, `test_near_duplicate_detector_groups_similar_questions`, and `test_duplicate_clusters_include_member_ids_and_reason`. Use fixed fixtures and deterministic thresholds only.</action>
  <verify>python3 -m pytest tests/unit/test_qc_dedup.py -q</verify>
  <acceptance_criteria>
    - `tests/unit/test_qc_dedup.py` contains `def test_exact_duplicate_detector_flags_same_canonical_signature`
    - `tests/unit/test_qc_dedup.py` contains `def test_near_duplicate_detector_groups_similar_questions`
  </acceptance_criteria>
  <done>Duplicate detection is tested against exact and near-duplicate cases.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
  <boundary>
    <name>Accepted candidates to deduplication pipeline</name>
    <description>Samples that survived validation and QC rules cross into duplicate detection before final export approval.</description>
    <data_crossing>Canonical sample content, prompt text fragments, answer text, scenario ids, latent signatures, cluster metadata</data_crossing>
  </boundary>
</trust_boundaries>
<threat_register>
  <threat>
    <threat_id>T-4-05</threat_id>
    <category>Integrity</category>
    <component>exact duplicate detection</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Derive stable fingerprints from canonical sample semantics so identical items are caught regardless of runtime ordering.</mitigation_plan>
    <files_to_check>src/smdgf/qc/dedup.py, tests/unit/test_qc_dedup.py</files_to_check>
  </threat>
  <threat>
    <threat_id>T-4-06</threat_id>
    <category>Integrity</category>
    <component>near-duplicate clustering</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Use deterministic similarity heuristics and explicit thresholds so near-duplicate grouping is reproducible and auditable.</mitigation_plan>
    <files_to_check>src/smdgf/qc/dedup.py, tests/unit/test_qc_dedup.py</files_to_check>
  </threat>
</threat_register>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `python3 -m pytest tests/unit/test_qc_dedup.py -q` succeeds
- [ ] Exact duplicate detection uses stable canonical fingerprints
- [ ] Near-duplicate grouping preserves deterministic cluster evidence
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Duplicate detection can prevent repeated accepted samples from reaching export
</success_criteria>

<output>
After completion, create `.planning/phases/04-quality-control-pipeline/04-03-SUMMARY.md`
</output>
