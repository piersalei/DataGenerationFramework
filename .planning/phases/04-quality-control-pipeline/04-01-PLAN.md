---
phase: 04-quality-control-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/smdgf/qc/__init__.py
  - src/smdgf/qc/models.py
  - src/smdgf/qc/rules.py
  - tests/unit/test_qc_rules.py
autonomous: true
requirements: [QUAL-01, QUAL-02]
user_setup: []
must_haves:
  truths:
    - "Every generated candidate can be validated structurally before export."
    - "Deterministic QC rules can reject malformed samples, answer leakage, and latent inconsistency with explicit reasons."
    - "Hard QC failures do not require additional model calls."
  artifacts:
    - path: "src/smdgf/qc/models.py"
      provides: "QC-facing candidate, finding, and decision contracts"
      min_lines: 90
      contains: "class QualityCandidate"
    - path: "src/smdgf/qc/rules.py"
      provides: "Schema validation and deterministic QC rule engine"
      min_lines: 80
      contains: "class RuleEngine"
    - path: "tests/unit/test_qc_rules.py"
      provides: "Validation and deterministic rule coverage"
      min_lines: 35
      contains: "def test_rule_engine_rejects_answer_leakage"
  key_links:
    - from: "src/smdgf/qc/rules.py"
      to: "src/smdgf/qc/models.py"
      via: "rule engine emits structured findings and QC decisions over QC candidate records"
      pattern: "QualityCandidate|QualityFinding|QualityDecision"
    - from: "src/smdgf/qc/models.py"
      to: "src/smdgf/schemas/canonical.py"
      via: "QC contracts wrap canonical samples instead of export-specific formats"
      pattern: "CanonicalSample"
---

<objective>
Implement the base QC contracts, candidate validation, and deterministic rule engine.

Purpose: Phase 4 needs reproducible hard checks before optional judge-based filtering or downstream export.
Output: QC models, schema validation helpers, deterministic rule engine, and unit tests for structural and leakage failures.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-quality-control-pipeline/04-RESEARCH.md
@src/smdgf/schemas/canonical.py
@src/smdgf/generation/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add QC contracts and package exports</name>
  <files>src/smdgf/qc/__init__.py, src/smdgf/qc/models.py</files>
  <read_first>.planning/phases/04-quality-control-pipeline/04-RESEARCH.md, .planning/REQUIREMENTS.md, src/smdgf/schemas/canonical.py, src/smdgf/generation/models.py</read_first>
  <action>Create `src/smdgf/qc/__init__.py` and `src/smdgf/qc/models.py`. Define strict Pydantic models for `QualityCandidate`, `QualityFinding`, `QualityDecision`, `RuleResult`, `ReviewDisposition`, and any enums or evidence payloads needed to represent `accept`, `reject`, and `review` outcomes. Ensure QC contracts link a canonical sample to generation provenance and can serialize deterministically.</action>
  <verify>python3 - <<'PY'\nfrom pathlib import Path\nchecks = {\n  'src/smdgf/qc/models.py': ['class QualityCandidate', 'class QualityFinding', 'class QualityDecision'],\n  'src/smdgf/qc/__init__.py': ['QualityCandidate', 'QualityDecision'],\n}\nfor path, needles in checks.items():\n    text = Path(path).read_text()\n    if any(n not in text for n in needles):\n        raise SystemExit(1)\nPY</verify>
  <acceptance_criteria>
    - `src/smdgf/qc/models.py` contains `class QualityCandidate`
    - `src/smdgf/qc/models.py` contains `class QualityDecision`
    - `src/smdgf/qc/__init__.py` exports QC contract symbols
  </acceptance_criteria>
  <done>QC contracts exist and are ready for rule evaluation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement deterministic validation and rule engine with unit tests</name>
  <files>src/smdgf/qc/rules.py, tests/unit/test_qc_rules.py</files>
  <read_first>.planning/phases/04-quality-control-pipeline/04-VALIDATION.md, src/smdgf/qc/models.py, src/smdgf/schemas/canonical.py, tests/conftest.py</read_first>
  <action>Create `src/smdgf/qc/rules.py` with schema-validation helpers plus a deterministic `RuleEngine` that can run rules for structural errors, answer leakage, and latent-state consistency. Add `tests/unit/test_qc_rules.py` with tests named `test_quality_candidate_validation_rejects_missing_fields`, `test_rule_engine_rejects_answer_leakage`, and `test_rule_engine_rejects_latent_inconsistency`. Use fixtures or inline canonical samples only. Do not call any model providers in this plan.</action>
  <verify>python3 -m pytest tests/unit/test_qc_rules.py -q</verify>
  <acceptance_criteria>
    - `src/smdgf/qc/rules.py` contains `class RuleEngine`
    - `tests/unit/test_qc_rules.py` contains `def test_rule_engine_rejects_answer_leakage`
    - `tests/unit/test_qc_rules.py` contains `def test_rule_engine_rejects_latent_inconsistency`
  </acceptance_criteria>
  <done>Deterministic QC now rejects malformed or invalid candidates with explicit findings.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
  <boundary>
    <name>Canonical candidates to QC decisions</name>
    <description>Generated canonical candidates cross into the QC subsystem where they are validated and either accepted, rejected, or flagged for review.</description>
    <data_crossing>Canonical sample fields, latent states, answer contract, prompt provenance, QC findings</data_crossing>
  </boundary>
</trust_boundaries>
<threat_register>
  <threat>
    <threat_id>T-4-01</threat_id>
    <category>Tampering</category>
    <component>candidate validation</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Validate QC candidates against strict contracts before any export or reporting step so malformed records cannot silently pass through.</mitigation_plan>
    <files_to_check>src/smdgf/qc/models.py, src/smdgf/qc/rules.py, tests/unit/test_qc_rules.py</files_to_check>
  </threat>
  <threat>
    <threat_id>T-4-02</threat_id>
    <category>Integrity</category>
    <component>deterministic QC rules</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Express leakage and latent-consistency checks as deterministic rules with structured evidence so failures are reproducible and auditable.</mitigation_plan>
    <files_to_check>src/smdgf/qc/rules.py, tests/unit/test_qc_rules.py</files_to_check>
  </threat>
</threat_register>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `python3 -m pytest tests/unit/test_qc_rules.py -q` succeeds
- [ ] QC validation rejects malformed candidates before export
- [ ] Rule engine emits explicit structured reasons for deterministic rejections
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Deterministic QC exists before any judge-based filtering is added
</success_criteria>

<output>
After completion, create `.planning/phases/04-quality-control-pipeline/04-01-SUMMARY.md`
</output>
