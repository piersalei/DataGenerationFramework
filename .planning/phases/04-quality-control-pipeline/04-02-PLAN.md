---
phase: 04-quality-control-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/smdgf/qc/models.py
  - src/smdgf/qc/judges.py
  - tests/unit/test_qc_judges.py
autonomous: true
requirements: [QUAL-03]
user_setup: []
must_haves:
  truths:
    - "QC supports configurable score-based or judge-based filtering in addition to deterministic rules."
    - "Judge outputs are normalized behind framework-owned interfaces and result models."
    - "Judge thresholds and aggregation remain reproducible from config."
  artifacts:
    - path: "src/smdgf/qc/judges.py"
      provides: "QualityJudge protocol, normalized score or verdict models, and threshold helpers"
      min_lines: 80
      contains: "class QualityJudge"
    - path: "tests/unit/test_qc_judges.py"
      provides: "Judge extension and threshold behavior tests"
      min_lines: 35
      contains: "def test_threshold_judge_marks_borderline_sample_for_review"
  key_links:
    - from: "src/smdgf/qc/judges.py"
      to: "src/smdgf/qc/models.py"
      via: "judge modules emit normalized findings and decisions into QC contracts"
      pattern: "QualityDecision|QualityFinding"
    - from: "src/smdgf/qc/judges.py"
      to: "src/smdgf/generation/providers.py"
      via: "judge adapters may be model-backed later without coupling QC core to one provider"
      pattern: "LiteLLMGenerationProvider|GenerationProvider"
---

<objective>
Implement configurable judge and score-based QC extension points.

Purpose: Deterministic rules are necessary but not sufficient for softer quality judgments such as ambiguity or explanation adequacy.
Output: Judge interfaces, normalized judge result models, threshold policy helpers, and unit tests for score-based and verdict-based filtering.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-quality-control-pipeline/04-RESEARCH.md
@.planning/phases/04-quality-control-pipeline/04-01-PLAN.md
@src/smdgf/qc/models.py
@src/smdgf/generation/providers.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add judge and score extension interfaces</name>
  <files>src/smdgf/qc/models.py, src/smdgf/qc/judges.py</files>
  <read_first>.planning/phases/04-quality-control-pipeline/04-RESEARCH.md, src/smdgf/qc/models.py, src/smdgf/generation/providers.py</read_first>
  <action>Extend `src/smdgf/qc/models.py` as needed and create `src/smdgf/qc/judges.py` with a `QualityJudge` protocol plus normalized judge request, verdict, score, and aggregation helpers. Support deterministic threshold evaluation for `accept`, `review`, and `reject` routing without hardwiring one provider backend. Keep provider integration optional or stubbed in this plan.</action>
  <verify>python3 - <<'PY'\nfrom pathlib import Path\nchecks = {\n  'src/smdgf/qc/judges.py': ['class QualityJudge', 'class JudgeResult', 'def apply_thresholds'],\n}\nfor path, needles in checks.items():\n    text = Path(path).read_text()\n    if any(n not in text for n in needles):\n        raise SystemExit(1)\nPY</verify>
  <acceptance_criteria>
    - `src/smdgf/qc/judges.py` contains `class QualityJudge`
    - `src/smdgf/qc/judges.py` contains `class JudgeResult`
    - `src/smdgf/qc/judges.py` contains `def apply_thresholds`
  </acceptance_criteria>
  <done>QC can now host judge-style evaluation without polluting deterministic rule logic.</done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for threshold routing and normalized judge outputs</name>
  <files>tests/unit/test_qc_judges.py</files>
  <read_first>.planning/phases/04-quality-control-pipeline/04-VALIDATION.md, src/smdgf/qc/models.py, src/smdgf/qc/judges.py, tests/conftest.py</read_first>
  <action>Create `tests/unit/test_qc_judges.py` with tests named `test_score_based_judge_result_normalizes_provenance`, `test_threshold_judge_marks_borderline_sample_for_review`, and `test_judge_aggregation_can_reject_low_confidence_candidate`. Use stub judges and deterministic scores only. No network calls or real model execution.</action>
  <verify>python3 -m pytest tests/unit/test_qc_judges.py -q</verify>
  <acceptance_criteria>
    - `tests/unit/test_qc_judges.py` contains `def test_threshold_judge_marks_borderline_sample_for_review`
    - `tests/unit/test_qc_judges.py` contains `def test_judge_aggregation_can_reject_low_confidence_candidate`
  </acceptance_criteria>
  <done>Judge extension points are verified and reproducible under fixed thresholds.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
  <boundary>
    <name>QC core to optional judge implementations</name>
    <description>Optional judges may score or classify QC candidates, but their outputs must enter the QC pipeline through normalized models and explicit thresholds.</description>
    <data_crossing>QC candidate data, judge prompts or rubric inputs, scores, verdicts, explanations, provider provenance</data_crossing>
  </boundary>
</trust_boundaries>
<threat_register>
  <threat>
    <threat_id>T-4-03</threat_id>
    <category>Integrity</category>
    <component>judge interface</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Hide judge implementations behind a framework-owned protocol so soft QC remains configurable and comparable across providers.</mitigation_plan>
    <files_to_check>src/smdgf/qc/judges.py, tests/unit/test_qc_judges.py</files_to_check>
  </threat>
  <threat>
    <threat_id>T-4-04</threat_id>
    <category>Repudiation</category>
    <component>threshold routing</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Persist judge scores, thresholds, and explanations in normalized results so accept or review decisions are reproducible from config.</mitigation_plan>
    <files_to_check>src/smdgf/qc/models.py, src/smdgf/qc/judges.py, tests/unit/test_qc_judges.py</files_to_check>
  </threat>
</threat_register>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `python3 -m pytest tests/unit/test_qc_judges.py -q` succeeds
- [ ] Judge outputs are normalized into framework-owned result models
- [ ] Threshold logic can route candidates to `accept`, `review`, or `reject` deterministically
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Judge-based QC remains optional, configurable, and auditable
</success_criteria>

<output>
After completion, create `.planning/phases/04-quality-control-pipeline/04-02-SUMMARY.md`
</output>
