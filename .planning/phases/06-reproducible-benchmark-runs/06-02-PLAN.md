---
phase: 06-reproducible-benchmark-runs
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/smdgf/benchmark/models.py
  - src/smdgf/benchmark/tracker.py
  - tests/unit/test_benchmark_tracker.py
autonomous: true
requirements: [REPR-02]
user_setup: []
must_haves:
  truths:
    - "Run tracking stores parameters, metrics, and artifact references for later comparison."
    - "Tracking works locally by default and does not require a remote service."
    - "Optional tracking adapters can be added without changing the benchmark core contracts."
  artifacts:
    - path: "src/smdgf/benchmark/tracker.py"
      provides: "Local-first run tracker and optional adapter hooks"
      min_lines: 85
      contains: "class RunTracker"
    - path: "tests/unit/test_benchmark_tracker.py"
      provides: "Tracking and comparison tests"
      min_lines: 40
      contains: "def test_local_tracker_records_params_metrics_and_artifacts"
  key_links:
    - from: "src/smdgf/benchmark/tracker.py"
      to: "src/smdgf/benchmark/models.py"
      via: "tracking persists benchmark manifest references and summary metrics"
      pattern: "BenchmarkRunManifest|artifact_refs"
    - from: "src/smdgf/benchmark/tracker.py"
      to: "src/smdgf/export/manifest.py"
      via: "run comparison remains grounded in export artifact references"
      pattern: "ExportRunManifest|run_id"
---

<objective>
Implement local-first run tracking plus optional adapter hooks for experiment comparison.

Purpose: Reproducible benchmark runs need comparison-friendly tracking, but the framework must still work entirely locally for the team.
Output: Run tracker contracts, local JSON tracker, optional adapter hook surface, and tracking unit tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-reproducible-benchmark-runs/06-RESEARCH.md
@.planning/phases/06-reproducible-benchmark-runs/06-01-PLAN.md
@src/smdgf/benchmark/models.py
@src/smdgf/export/manifest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add local-first tracking interfaces and persistence helpers</name>
  <files>src/smdgf/benchmark/models.py, src/smdgf/benchmark/tracker.py</files>
  <read_first>.planning/phases/06-reproducible-benchmark-runs/06-RESEARCH.md, src/smdgf/benchmark/models.py, src/smdgf/export/manifest.py</read_first>
  <action>Extend `src/smdgf/benchmark/models.py` as needed and create `src/smdgf/benchmark/tracker.py` with a `RunTracker` protocol or base class, a local JSON-backed tracker implementation, and optional adapter hook points for later MLflow-style integrations. Ensure tracked runs preserve parameters, metrics, tags, and artifact references explicitly.</action>
  <verify>python3 - <<'PY'\nfrom pathlib import Path\nchecks = {\n  'src/smdgf/benchmark/tracker.py': ['class RunTracker', 'class LocalRunTracker', 'def compare_runs'],\n}\nfor path, needles in checks.items():\n    text = Path(path).read_text()\n    if any(n not in text for n in needles):\n        raise SystemExit(1)\nPY</verify>
  <acceptance_criteria>
    - `src/smdgf/benchmark/tracker.py` contains `class RunTracker`
    - `src/smdgf/benchmark/tracker.py` contains `class LocalRunTracker`
    - `src/smdgf/benchmark/tracker.py` contains `def compare_runs`
  </acceptance_criteria>
  <done>Benchmark runs can now be tracked locally with explicit metrics and artifact references.</done>
</task>

<task type="auto">
  <name>Task 2: Add benchmark tracking and comparison tests</name>
  <files>tests/unit/test_benchmark_tracker.py</files>
  <read_first>.planning/phases/06-reproducible-benchmark-runs/06-VALIDATION.md, src/smdgf/benchmark/models.py, src/smdgf/benchmark/tracker.py, tests/conftest.py</read_first>
  <action>Create `tests/unit/test_benchmark_tracker.py` with tests named `test_local_tracker_records_params_metrics_and_artifacts`, `test_compare_runs_surfaces_metric_deltas`, and `test_tracker_round_trip_preserves_tags`. Use temporary files only and avoid any network or external tracking service.</action>
  <verify>python3 -m pytest tests/unit/test_benchmark_tracker.py -q</verify>
  <acceptance_criteria>
    - `tests/unit/test_benchmark_tracker.py` contains `def test_local_tracker_records_params_metrics_and_artifacts`
    - `tests/unit/test_benchmark_tracker.py` contains `def test_compare_runs_surfaces_metric_deltas`
  </acceptance_criteria>
  <done>Run tracking and comparison hooks are now covered by deterministic tests.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
  <boundary>
    <name>Benchmark run state to tracking backend</name>
    <description>Benchmark parameters, metrics, and artifact references cross into tracking helpers that may later target local or optional external backends.</description>
    <data_crossing>run ids, params, metrics, tags, artifact references, comparison summaries</data_crossing>
  </boundary>
</trust_boundaries>
<threat_register>
  <threat>
    <threat_id>T-6-03</threat_id>
    <category>Repudiation</category>
    <component>local run tracking</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Track runs in a local-first structured format so comparisons and audits work even without a remote service.</mitigation_plan>
    <files_to_check>src/smdgf/benchmark/tracker.py, tests/unit/test_benchmark_tracker.py</files_to_check>
  </threat>
  <threat>
    <threat_id>T-6-04</threat_id>
    <category>Availability</category>
    <component>optional adapter hooks</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Keep external tracking adapters optional so core reproducibility remains available under local-only execution.</mitigation_plan>
    <files_to_check>src/smdgf/benchmark/models.py, src/smdgf/benchmark/tracker.py, tests/unit/test_benchmark_tracker.py</files_to_check>
  </threat>
</threat_register>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `python3 -m pytest tests/unit/test_benchmark_tracker.py -q` succeeds
- [ ] Local tracking preserves parameters, metrics, tags, and artifact references
- [ ] Run comparison does not depend on a remote backend
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Benchmark tracking is local-first, comparison-friendly, and adapter-ready
</success_criteria>

<output>
After completion, create `.planning/phases/06-reproducible-benchmark-runs/06-02-SUMMARY.md`
</output>
