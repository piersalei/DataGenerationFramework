---
phase: 06-reproducible-benchmark-runs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/smdgf/benchmark/__init__.py
  - src/smdgf/benchmark/models.py
  - tests/unit/test_benchmark_manifest.py
autonomous: true
requirements: [REPR-01]
user_setup: []
must_haves:
  truths:
    - "A benchmark run can be reconstructed from structured manifest references and config snapshots."
    - "Benchmark manifests stitch together generation, QC, and export artifacts without duplicating their full payloads."
    - "Benchmark artifact layouts remain deterministic and DVC-friendly."
  artifacts:
    - path: "src/smdgf/benchmark/models.py"
      provides: "Benchmark-run manifest and artifact-reference contracts"
      min_lines: 90
      contains: "class BenchmarkRunManifest"
    - path: "tests/unit/test_benchmark_manifest.py"
      provides: "Manifest stitching and layout tests"
      min_lines: 40
      contains: "def test_benchmark_manifest_stitches_generation_qc_and_export_refs"
  key_links:
    - from: "src/smdgf/benchmark/models.py"
      to: "src/smdgf/generation/models.py"
      via: "benchmark manifest references generation run artifacts instead of re-encoding them"
      pattern: "GenerationRunManifest|run_id"
    - from: "src/smdgf/benchmark/models.py"
      to: "src/smdgf/export/manifest.py"
      via: "benchmark manifest stitches export outputs into one benchmark run record"
      pattern: "ExportRunManifest|artifact_paths"
---

<objective>
Integrate benchmark-run manifests and deterministic artifact-layout stitching.

Purpose: Reproducible benchmark runs need one top-level manifest that ties together generation, QC, export, config, and seed references.
Output: Benchmark-run manifest contracts, deterministic benchmark layout helpers, and manifest-focused unit tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-reproducible-benchmark-runs/06-RESEARCH.md
@src/smdgf/generation/models.py
@src/smdgf/qc/reports.py
@src/smdgf/export/manifest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add benchmark-run manifest contracts and package exports</name>
  <files>src/smdgf/benchmark/__init__.py, src/smdgf/benchmark/models.py</files>
  <read_first>.planning/phases/06-reproducible-benchmark-runs/06-RESEARCH.md, .planning/REQUIREMENTS.md, src/smdgf/generation/models.py, src/smdgf/qc/reports.py, src/smdgf/export/manifest.py</read_first>
  <action>Create `src/smdgf/benchmark/__init__.py` and `src/smdgf/benchmark/models.py`. Define strict Pydantic models for `BenchmarkRunManifest`, artifact references, code/config snapshots, and seed inventories. Keep the benchmark manifest referential: it should point to generation, QC, and export artifacts instead of embedding their entire contents.</action>
  <verify>python3 - <<'PY'\nfrom pathlib import Path\nchecks = {\n  'src/smdgf/benchmark/models.py': ['class BenchmarkRunManifest', 'config_snapshot', 'artifact_refs'],\n  'src/smdgf/benchmark/__init__.py': ['BenchmarkRunManifest'],\n}\nfor path, needles in checks.items():\n    text = Path(path).read_text()\n    if any(n not in text for n in needles):\n        raise SystemExit(1)\nPY</verify>
  <acceptance_criteria>
    - `src/smdgf/benchmark/models.py` contains `class BenchmarkRunManifest`
    - `src/smdgf/benchmark/models.py` preserves config and artifact-reference fields
    - `src/smdgf/benchmark/__init__.py` exports benchmark manifest symbols
  </acceptance_criteria>
  <done>Benchmark-run contracts now exist to stitch earlier phase artifacts together.</done>
</task>

<task type="auto">
  <name>Task 2: Add benchmark manifest and layout tests</name>
  <files>tests/unit/test_benchmark_manifest.py</files>
  <read_first>.planning/phases/06-reproducible-benchmark-runs/06-VALIDATION.md, src/smdgf/benchmark/models.py, src/smdgf/export/manifest.py, tests/conftest.py</read_first>
  <action>Create `tests/unit/test_benchmark_manifest.py` with tests named `test_benchmark_manifest_stitches_generation_qc_and_export_refs`, `test_benchmark_layout_is_deterministic_by_run_id`, and `test_benchmark_manifest_round_trip_preserves_config_snapshot`. Use fixture-style manifest references and temporary directories only.</action>
  <verify>python3 -m pytest tests/unit/test_benchmark_manifest.py -q</verify>
  <acceptance_criteria>
    - `tests/unit/test_benchmark_manifest.py` contains `def test_benchmark_manifest_stitches_generation_qc_and_export_refs`
    - `tests/unit/test_benchmark_manifest.py` contains `def test_benchmark_manifest_round_trip_preserves_config_snapshot`
  </acceptance_criteria>
  <done>Benchmark manifests and layout helpers are now covered by deterministic tests.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
  <boundary>
    <name>Phase artifacts to benchmark-run manifest</name>
    <description>Generation, QC, and export artifacts cross into one benchmark-run manifest that future replay and comparison workflows depend on.</description>
    <data_crossing>artifact refs, config snapshot, code revision, seed inventory, manifest paths</data_crossing>
  </boundary>
</trust_boundaries>
<threat_register>
  <threat>
    <threat_id>T-6-01</threat_id>
    <category>Repudiation</category>
    <component>benchmark manifest stitching</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Use one explicit benchmark-run manifest to reference generation, QC, and export artifacts so a build can be reconstructed without guessing file relationships.</mitigation_plan>
    <files_to_check>src/smdgf/benchmark/models.py, tests/unit/test_benchmark_manifest.py</files_to_check>
  </threat>
  <threat>
    <threat_id>T-6-02</threat_id>
    <category>Repudiation</category>
    <component>artifact layout determinism</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Keep benchmark run layouts deterministic and run-id based so DVC-friendly artifact tracking and replay stay stable across runs.</mitigation_plan>
    <files_to_check>src/smdgf/benchmark/models.py, tests/unit/test_benchmark_manifest.py</files_to_check>
  </threat>
</threat_register>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `python3 -m pytest tests/unit/test_benchmark_manifest.py -q` succeeds
- [ ] Benchmark manifests stitch generation, QC, export, config, and seed references explicitly
- [ ] Benchmark layouts are deterministic by run id
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Benchmark-run manifests can reconstruct the artifact chain from generation through export
</success_criteria>

<output>
After completion, create `.planning/phases/06-reproducible-benchmark-runs/06-01-SUMMARY.md`
</output>
