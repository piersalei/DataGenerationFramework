---
phase: 06-reproducible-benchmark-runs
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - src/smdgf/benchmark/taskpack.py
  - tests/unit/test_taskpack_smoke.py
autonomous: true
requirements: [REPR-01, REPR-02]
user_setup: []
must_haves:
  truths:
    - "The repo includes at least one baseline internal task pack."
    - "The baseline task pack exercises generation, QC, export, and benchmark tracking boundaries end to end."
    - "End-to-end smoke verification remains lightweight and local-test friendly."
  artifacts:
    - path: "src/smdgf/benchmark/taskpack.py"
      provides: "Baseline task-pack metadata and smoke helpers"
      min_lines: 85
      contains: "class TaskPack"
    - path: "tests/unit/test_taskpack_smoke.py"
      provides: "Baseline task-pack smoke tests"
      min_lines: 45
      contains: "def test_baseline_taskpack_smoke_produces_benchmark_run_refs"
  key_links:
    - from: "src/smdgf/benchmark/taskpack.py"
      to: "src/smdgf/benchmark/models.py"
      via: "baseline task pack produces reproducible benchmark run references"
      pattern: "BenchmarkRunManifest|TaskPack"
    - from: "tests/unit/test_taskpack_smoke.py"
      to: "src/smdgf/export/manifest.py"
      via: "smoke flow validates end-to-end boundary from export artifacts into benchmark packaging"
      pattern: "ExportRunManifest|artifact_paths"
---

<objective>
Ship a baseline internal task pack and end-to-end smoke tests.

Purpose: Phase 6 only proves its value if one baseline task pack can exercise the benchmark workflow from artifact references through packaged benchmark-run metadata.
Output: Task-pack metadata models, lightweight smoke helpers, and end-to-end fixture-backed tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-reproducible-benchmark-runs/06-RESEARCH.md
@.planning/phases/06-reproducible-benchmark-runs/06-02-PLAN.md
@src/smdgf/benchmark/models.py
@src/smdgf/benchmark/tracker.py
@src/smdgf/export/manifest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add baseline task-pack models and smoke helpers</name>
  <files>src/smdgf/benchmark/taskpack.py</files>
  <read_first>.planning/phases/06-reproducible-benchmark-runs/06-RESEARCH.md, src/smdgf/benchmark/models.py, src/smdgf/benchmark/tracker.py, src/smdgf/export/manifest.py</read_first>
  <action>Create `src/smdgf/benchmark/taskpack.py` with strict models and helpers for a baseline internal task pack, including task-pack ids, referenced task ids, source fixture paths, and a lightweight smoke helper that assembles benchmark-run references without calling remote providers. Keep the pack representative of the end-to-end pipeline, not a special-case shortcut.</action>
  <verify>python3 - <<'PY'\nfrom pathlib import Path\nchecks = {\n  'src/smdgf/benchmark/taskpack.py': ['class TaskPack', 'def build_baseline_taskpack', 'def smoke_taskpack_run'],\n}\nfor path, needles in checks.items():\n    text = Path(path).read_text()\n    if any(n not in text for n in needles):\n        raise SystemExit(1)\nPY</verify>
  <acceptance_criteria>
    - `src/smdgf/benchmark/taskpack.py` contains `class TaskPack`
    - `src/smdgf/benchmark/taskpack.py` contains `def build_baseline_taskpack`
    - `src/smdgf/benchmark/taskpack.py` contains `def smoke_taskpack_run`
  </acceptance_criteria>
  <done>The repo now has a baseline task-pack representation for reproducible benchmark smoke flows.</done>
</task>

<task type="auto">
  <name>Task 2: Add task-pack smoke tests</name>
  <files>tests/unit/test_taskpack_smoke.py</files>
  <read_first>.planning/phases/06-reproducible-benchmark-runs/06-VALIDATION.md, src/smdgf/benchmark/taskpack.py, src/smdgf/export/manifest.py, tests/conftest.py</read_first>
  <action>Create `tests/unit/test_taskpack_smoke.py` with tests named `test_baseline_taskpack_smoke_produces_benchmark_run_refs`, `test_baseline_taskpack_lists_task_ids_and_fixture_paths`, and `test_smoke_taskpack_run_records_tracking_summary`. Use fixture-backed manifests and local smoke data only.</action>
  <verify>python3 -m pytest tests/unit/test_taskpack_smoke.py -q</verify>
  <acceptance_criteria>
    - `tests/unit/test_taskpack_smoke.py` contains `def test_baseline_taskpack_smoke_produces_benchmark_run_refs`
    - `tests/unit/test_taskpack_smoke.py` contains `def test_smoke_taskpack_run_records_tracking_summary`
  </acceptance_criteria>
  <done>Baseline task-pack and end-to-end smoke behavior are now verified locally.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
  <boundary>
    <name>Baseline task pack to benchmark smoke workflow</name>
    <description>Task-pack metadata and artifact references cross into an end-to-end smoke path that must reflect the real benchmark workflow without remote dependencies.</description>
    <data_crossing>task-pack ids, task ids, fixture paths, benchmark manifest refs, tracking summaries</data_crossing>
  </boundary>
</trust_boundaries>
<threat_register>
  <threat>
    <threat_id>T-6-05</threat_id>
    <category>Integrity</category>
    <component>baseline task-pack packaging</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Represent baseline task packs explicitly with source task ids and fixture references so benchmark examples remain inspectable and reproducible.</mitigation_plan>
    <files_to_check>src/smdgf/benchmark/taskpack.py, tests/unit/test_taskpack_smoke.py</files_to_check>
  </threat>
  <threat>
    <threat_id>T-6-06</threat_id>
    <category>Integrity</category>
    <component>end-to-end smoke workflow</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Use fixture-backed smoke helpers that preserve generation/QC/export/benchmark boundaries instead of shortcutting around the real artifact chain.</mitigation_plan>
    <files_to_check>src/smdgf/benchmark/taskpack.py, tests/unit/test_taskpack_smoke.py</files_to_check>
  </threat>
</threat_register>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `python3 -m pytest tests/unit/test_taskpack_smoke.py -q` succeeds
- [ ] A baseline task pack exists with explicit task ids and fixture references
- [ ] Smoke workflow produces benchmark-run and tracking summaries without remote services
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo ships one baseline task pack that exercises the full benchmark workflow locally
</success_criteria>

<output>
After completion, create `.planning/phases/06-reproducible-benchmark-runs/06-03-SUMMARY.md`
</output>
