---
phase: 01-canonical-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/smdgf/__init__.py
  - src/smdgf/cli/__init__.py
  - src/smdgf/config/__init__.py
  - src/smdgf/schemas/__init__.py
  - tests/conftest.py
  - tests/unit/test_contract_scaffold.py
autonomous: true
requirements: [TASK-01, TASK-04]
user_setup: []
must_haves:
  truths:
    - "Project installs as a Python package with pinned contract-layer dependencies."
    - "The src/smdgf package and its contract-related subpackages can be imported by tests."
    - "Wave 1 test scaffolding exists so later contract and CLI plans can add real assertions without reworking infrastructure."
  artifacts:
    - path: "pyproject.toml"
      provides: "Python package metadata, dependency pins, and pytest configuration"
      min_lines: 25
      contains: "[project]"
    - path: "src/smdgf/__init__.py"
      provides: "Top-level package marker and exported version surface"
      min_lines: 3
    - path: "tests/unit/test_contract_scaffold.py"
      provides: "Scaffold smoke tests for package imports"
      min_lines: 10
      contains: "def test_package_imports"
  key_links:
    - from: "pyproject.toml"
      to: "src/smdgf"
      via: "editable package configuration"
      pattern: "package-dir|tool\\.setuptools|src"
    - from: "tests/unit/test_contract_scaffold.py"
      to: "src/smdgf/__init__.py"
      via: "import smoke test"
      pattern: "import smdgf|from smdgf import"
---

<objective>
Create the Python package scaffold, dependency configuration, and baseline test harness for the canonical-contract layer.

Purpose: Later plans need a stable package root, deterministic dependencies, and runnable tests before implementing schemas and CLI commands.
Output: `pyproject.toml`, `src/smdgf` package markers, and baseline unit-test scaffolding.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-canonical-contracts/01-RESEARCH.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Python project metadata and dependency pins</name>
  <files>pyproject.toml</files>
  <read_first>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/phases/01-canonical-contracts/01-RESEARCH.md, AGENTS.md</read_first>
  <action>Create `pyproject.toml` with Python requirement `>=3.11`, package name `smdgf`, version `0.1.0`, and dependency pins for `pydantic>=2.12,<3`, `typer>=0.24,<0.25`, `hydra-core>=1.3,<1.4`, and `pyyaml>=6,<7`. Add optional dev dependencies for `pytest>=8,<9` and `ruff>=0.12,<0.13`. Configure setuptools to use the `src` layout, add `python -m pytest -q` compatibility through pytest config, and add a console script entry point `smdgf=smdgf.cli.main:app`.</action>
  <verify>python - <<'PY'\nfrom pathlib import Path\ntext = Path('pyproject.toml').read_text()\nneedles = ['name = \"smdgf\"', 'requires-python = \">=3.11\"', 'pydantic>=2.12,<3', 'typer>=0.24,<0.25', 'hydra-core>=1.3,<1.4', 'smdgf.cli.main:app']\nmissing = [n for n in needles if n not in text]\nraise SystemExit(1 if missing else 0)\nPY</verify>
  <acceptance_criteria>
    - `pyproject.toml` contains `name = "smdgf"` and `requires-python = ">=3.11"`
    - `pyproject.toml` contains `pydantic>=2.12,<3`, `typer>=0.24,<0.25`, and `hydra-core>=1.3,<1.4`
    - `pyproject.toml` contains `smdgf = "smdgf.cli.main:app"`
  </acceptance_criteria>
  <done>Project metadata and dependency pins are present and grep-verifiable.</done>
</task>

<task type="auto">
  <name>Task 2: Create package markers and baseline scaffold tests</name>
  <files>src/smdgf/__init__.py, src/smdgf/cli/__init__.py, src/smdgf/config/__init__.py, src/smdgf/schemas/__init__.py, tests/conftest.py, tests/unit/test_contract_scaffold.py</files>
  <read_first>.planning/PROJECT.md, .planning/phases/01-canonical-contracts/01-RESEARCH.md, AGENTS.md</read_first>
  <action>Create the `src/smdgf` package root plus `cli`, `config`, and `schemas` subpackage markers. Export `__version__ = "0.1.0"` from `src/smdgf/__init__.py`. Add `tests/conftest.py` with a fixture that resolves the repository root and ensures the package import path is available. Add `tests/unit/test_contract_scaffold.py` with smoke tests named `test_package_imports` and `test_cli_entrypoint_module_exists` that import `smdgf` and `smdgf.cli.main` without using placeholder TODO assertions.</action>
  <verify>python - <<'PY'\nfrom pathlib import Path\nfiles = [\n  'src/smdgf/__init__.py',\n  'src/smdgf/cli/__init__.py',\n  'src/smdgf/config/__init__.py',\n  'src/smdgf/schemas/__init__.py',\n  'tests/conftest.py',\n  'tests/unit/test_contract_scaffold.py',\n]\nfor path in files:\n    if not Path(path).exists():\n        raise SystemExit(1)\ntext = Path('tests/unit/test_contract_scaffold.py').read_text()\nneedles = ['def test_package_imports', 'def test_cli_entrypoint_module_exists']\nraise SystemExit(1 if any(n not in text for n in needles) else 0)\nPY</verify>
  <acceptance_criteria>
    - `src/smdgf/__init__.py` contains `__version__ = "0.1.0"`
    - `tests/unit/test_contract_scaffold.py` contains `def test_package_imports`
    - `tests/unit/test_contract_scaffold.py` contains `def test_cli_entrypoint_module_exists`
  </acceptance_criteria>
  <done>Package markers and scaffold smoke tests exist and establish importable project roots.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
  <boundary>
    <name>Project metadata to executor environment</name>
    <description>`pyproject.toml` controls dependency resolution and CLI entrypoint exposure.</description>
    <data_crossing>Dependency versions, package metadata, executable entry points</data_crossing>
  </boundary>
</trust_boundaries>
<threat_register>
  <threat>
    <threat_id>T-1-01</threat_id>
    <category>Tampering</category>
    <component>pyproject.toml</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Pin dependency ranges explicitly and verify exact required strings in `pyproject.toml`.</mitigation_plan>
    <files_to_check>pyproject.toml</files_to_check>
  </threat>
  <threat>
    <threat_id>T-1-02</threat_id>
    <category>Spoofing</category>
    <component>package scaffold</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Create explicit package markers and smoke tests so imports fail fast if the package root is miswired.</mitigation_plan>
    <files_to_check>src/smdgf/__init__.py, tests/unit/test_contract_scaffold.py</files_to_check>
  </threat>
</threat_register>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/unit/test_contract_scaffold.py -q` succeeds
- [ ] `pyproject.toml` dependency pins and console script are present
- [ ] `src/smdgf` package markers and scaffold tests exist
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Python project scaffold is ready for contract-model implementation in the next wave
</success_criteria>

<output>
After completion, create `.planning/phases/01-canonical-contracts/01-01-SUMMARY.md`
</output>
