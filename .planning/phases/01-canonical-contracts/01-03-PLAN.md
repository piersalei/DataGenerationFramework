---
phase: 01-canonical-contracts
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/smdgf/cli/main.py
  - src/smdgf/cli/contracts.py
  - tests/unit/test_cli_contracts.py
  - tests/fixtures/task_definition_valid.yaml
  - tests/fixtures/task_definition_invalid.yaml
  - tests/fixtures/task_spec_valid.yaml
autonomous: true
requirements: [TASK-01, TASK-04]
user_setup: []
must_haves:
  truths:
    - "Researchers can inspect registered contracts from the CLI."
    - "Researchers can validate task definition and task specification files from the CLI."
    - "Invalid contract files fail with non-zero exit code and actionable validation output."
  artifacts:
    - path: "src/smdgf/cli/main.py"
      provides: "Typer application entrypoint"
      min_lines: 20
      contains: "app = typer.Typer"
    - path: "src/smdgf/cli/contracts.py"
      provides: "Contract inspect and validate commands"
      min_lines: 40
      contains: "@app.command"
    - path: "tests/unit/test_cli_contracts.py"
      provides: "CLI integration tests for inspect and validate"
      min_lines: 25
      contains: "def test_validate_accepts_valid_task_definition"
  key_links:
    - from: "src/smdgf/cli/contracts.py"
      to: "src/smdgf/registry.py"
      via: "inspect command lists registered task definitions"
      pattern: "TaskRegistry|registry"
    - from: "src/smdgf/cli/contracts.py"
      to: "src/smdgf/schemas/task.py"
      via: "validate command instantiates TaskDefinition"
      pattern: "TaskDefinition"
    - from: "tests/unit/test_cli_contracts.py"
      to: "tests/fixtures/task_definition_valid.yaml"
      via: "CLI fixture-driven validation"
      pattern: "task_definition_valid\\.yaml"
---

<objective>
Add CLI entry points for contract inspection and file validation so the Phase 1 contract layer is directly usable by researchers.

Purpose: Contract models are not enough if researchers cannot validate authored task files from the command line before later generation phases.
Output: Typer CLI commands, fixture files, and CLI-focused tests for inspect and validate behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-canonical-contracts/01-RESEARCH.md
@.planning/phases/01-canonical-contracts/01-02-PLAN.md
@src/smdgf/schemas/task.py
@src/smdgf/schemas/spec.py
@src/smdgf/registry.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Typer CLI entrypoint and contract subcommands</name>
  <files>src/smdgf/cli/main.py, src/smdgf/cli/contracts.py</files>
  <read_first>.planning/phases/01-canonical-contracts/01-RESEARCH.md, src/smdgf/schemas/task.py, src/smdgf/schemas/spec.py, src/smdgf/registry.py, pyproject.toml</read_first>
  <action>Create `src/smdgf/cli/main.py` with `app = typer.Typer(no_args_is_help=True)` and register a `contracts` sub-app from `src/smdgf/cli/contracts.py`. In `contracts.py`, implement `inspect` and `validate` commands. `inspect` should list registered task ids and names in deterministic sorted order. `validate` should accept `--kind task-definition|task-spec`, load YAML or JSON from a file path, instantiate either `TaskDefinition` or `TaskSpecification`, print `VALID` on success, and return a non-zero exit code plus validation details on failure. Do not use `eval`, dynamic imports from user input, or arbitrary file execution.</action>
  <verify>python - <<'PY'\nfrom pathlib import Path\nchecks = {\n  'src/smdgf/cli/main.py': ['app = typer.Typer', 'contracts'],\n  'src/smdgf/cli/contracts.py': ['def inspect', 'def validate', 'VALID'],\n}\nfor path, needles in checks.items():\n    text = Path(path).read_text()\n    if any(n not in text for n in needles):\n        raise SystemExit(1)\nPY</verify>
  <acceptance_criteria>
    - `src/smdgf/cli/main.py` contains `app = typer.Typer`
    - `src/smdgf/cli/contracts.py` contains `def inspect`
    - `src/smdgf/cli/contracts.py` contains `def validate`
    - `src/smdgf/cli/contracts.py` contains `VALID`
  </acceptance_criteria>
  <done>Contract CLI exists with inspect and validate commands that operate on the Phase 1 contract models.</done>
</task>

<task type="auto">
  <name>Task 2: Add CLI fixtures and executable validation tests</name>
  <files>tests/unit/test_cli_contracts.py, tests/fixtures/task_definition_valid.yaml, tests/fixtures/task_definition_invalid.yaml, tests/fixtures/task_spec_valid.yaml</files>
  <read_first>.planning/phases/01-canonical-contracts/01-VALIDATION.md, src/smdgf/cli/main.py, src/smdgf/cli/contracts.py, tests/conftest.py</read_first>
  <action>Create fixture files `tests/fixtures/task_definition_valid.yaml`, `tests/fixtures/task_definition_invalid.yaml`, and `tests/fixtures/task_spec_valid.yaml` with realistic Phase 1 contract payloads. Add `tests/unit/test_cli_contracts.py` using Typer's `CliRunner` or equivalent to assert four behaviors: `test_validate_accepts_valid_task_definition`, `test_validate_rejects_invalid_task_definition`, `test_validate_accepts_valid_task_spec`, and `test_inspect_lists_registered_tasks`. Ensure invalid input returns a non-zero exit code and emits a validation error string containing either `ValidationError` or `validation`.</action>
  <verify>python - <<'PY'\nfrom pathlib import Path\nchecks = {\n  'tests/unit/test_cli_contracts.py': [\n    'def test_validate_accepts_valid_task_definition',\n    'def test_validate_rejects_invalid_task_definition',\n    'def test_validate_accepts_valid_task_spec',\n    'def test_inspect_lists_registered_tasks',\n  ],\n  'tests/fixtures/task_definition_valid.yaml': ['task_id:', 'supported_exports:'],\n  'tests/fixtures/task_spec_valid.yaml': ['scene_templates:', 'quality_expectations:'],\n}\nfor path, needles in checks.items():\n    text = Path(path).read_text()\n    if any(n not in text for n in needles):\n        raise SystemExit(1)\nPY</verify>
  <acceptance_criteria>
    - `tests/unit/test_cli_contracts.py` contains `def test_validate_accepts_valid_task_definition`
    - `tests/unit/test_cli_contracts.py` contains `def test_inspect_lists_registered_tasks`
    - `tests/fixtures/task_definition_valid.yaml` contains `supported_exports:`
    - `tests/fixtures/task_spec_valid.yaml` contains `quality_expectations:`
  </acceptance_criteria>
  <done>CLI tests and fixtures exist for valid and invalid contract validation flows.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
  <boundary>
    <name>Research-authored contract files to CLI validator</name>
    <description>User-authored YAML or JSON files cross into the validation command.</description>
    <data_crossing>Task definitions, task specifications, validation error messages</data_crossing>
  </boundary>
</trust_boundaries>
<threat_register>
  <threat>
    <threat_id>T-1-06</threat_id>
    <category>Tampering</category>
    <component>CLI validate command</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Parse only YAML/JSON into Pydantic models and return explicit validation failures; never execute file content.</mitigation_plan>
    <files_to_check>src/smdgf/cli/contracts.py, tests/unit/test_cli_contracts.py</files_to_check>
  </threat>
  <threat>
    <threat_id>T-1-07</threat_id>
    <category>Repudiation</category>
    <component>CLI validation output</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Return deterministic success string `VALID` and non-zero exit on failures so callers can audit results unambiguously.</mitigation_plan>
    <files_to_check>src/smdgf/cli/contracts.py, tests/unit/test_cli_contracts.py</files_to_check>
  </threat>
</threat_register>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/unit/test_cli_contracts.py -q` succeeds
- [ ] `smdgf` CLI exposes `contracts inspect` and `contracts validate`
- [ ] Invalid contract fixture produces non-zero validation failure
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Researchers can inspect and validate Phase 1 contract files from the CLI
</success_criteria>

<output>
After completion, create `.planning/phases/01-canonical-contracts/01-03-SUMMARY.md`
</output>
