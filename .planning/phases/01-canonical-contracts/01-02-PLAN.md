---
phase: 01-canonical-contracts
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/smdgf/schemas/abilities.py
  - src/smdgf/schemas/task.py
  - src/smdgf/schemas/spec.py
  - src/smdgf/schemas/canonical.py
  - src/smdgf/registry.py
  - src/smdgf/schemas/__init__.py
  - tests/unit/test_contract_models.py
autonomous: true
requirements: [TASK-01, TASK-02, TASK-03, TASK-04]
user_setup: []
must_haves:
  truths:
    - "Researchers can define valid task contracts with constrained ability metadata."
    - "Structured task specifications can describe scene rules, question patterns, and QC expectations."
    - "Canonical samples exist as export-agnostic representations independent from MCQ, QA, or open QA renderers."
    - "A task registry can register, list, fetch, and reject duplicate task identifiers."
  artifacts:
    - path: "src/smdgf/schemas/task.py"
      provides: "TaskDefinition and related contract models"
      min_lines: 40
      contains: "class TaskDefinition"
    - path: "src/smdgf/schemas/spec.py"
      provides: "Structured task specification models"
      min_lines: 40
      contains: "class TaskSpecification"
    - path: "src/smdgf/schemas/canonical.py"
      provides: "Canonical sample representation"
      min_lines: 40
      contains: "class CanonicalSample"
    - path: "src/smdgf/registry.py"
      provides: "Task registry implementation"
      min_lines: 30
      contains: "class TaskRegistry"
    - path: "tests/unit/test_contract_models.py"
      provides: "Schema and registry validation coverage"
      min_lines: 25
      contains: "def test_registry_rejects_duplicate_task_id"
  key_links:
    - from: "src/smdgf/registry.py"
      to: "src/smdgf/schemas/task.py"
      via: "registry stores TaskDefinition instances"
      pattern: "TaskDefinition"
    - from: "src/smdgf/schemas/spec.py"
      to: "src/smdgf/schemas/task.py"
      via: "task specification references task identity and answer expectations"
      pattern: "task_id|answer_mode"
---

<objective>
Implement the canonical contract models and task registry that define the semantic backbone of the framework.

Purpose: Every later phase depends on stable task, specification, and canonical-sample contracts that reject malformed inputs early.
Output: Contract model modules under `src/smdgf/schemas`, a registry implementation, and focused unit tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-canonical-contracts/01-RESEARCH.md
@.planning/phases/01-canonical-contracts/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ability, task, and specification models</name>
  <files>src/smdgf/schemas/abilities.py, src/smdgf/schemas/task.py, src/smdgf/schemas/spec.py, src/smdgf/schemas/__init__.py</files>
  <read_first>.planning/phases/01-canonical-contracts/01-RESEARCH.md, .planning/REQUIREMENTS.md, .planning/phases/01-canonical-contracts/01-01-PLAN.md, src/smdgf/schemas/__init__.py</read_first>
  <action>Create Pydantic v2 models and enums for the contract layer. In `abilities.py`, define `AbilityCategory` with exact values `emotion`, `desire`, `intention`, `belief`, `knowledge`, `social_relation`, `non_literal`, `social_decision`, and `implicit_stance`, plus an `AbilityDescriptor` model. In `task.py`, define `AnswerMode`, `ExportFormat`, and `TaskDefinition` with fields `task_id`, `name`, `description`, `ability_category`, `sub_capabilities`, `latent_variables`, `answer_mode`, and `supported_exports`. In `spec.py`, define `SceneTemplateSpec`, `QuestionPatternSpec`, `QualityExpectationSpec`, and `TaskSpecification` so structured task specs can express scene rules, role constraints, question patterns, and QC expectations. Export these symbols from `src/smdgf/schemas/__init__.py`.</action>
  <verify>python - <<'PY'\nfrom pathlib import Path\nchecks = {\n  'src/smdgf/schemas/abilities.py': ['class AbilityCategory', 'implicit_stance'],\n  'src/smdgf/schemas/task.py': ['class TaskDefinition', 'supported_exports'],\n  'src/smdgf/schemas/spec.py': ['class TaskSpecification', 'class SceneTemplateSpec'],\n}\nfor path, needles in checks.items():\n    text = Path(path).read_text()\n    if any(n not in text for n in needles):\n        raise SystemExit(1)\nPY</verify>
  <acceptance_criteria>
    - `src/smdgf/schemas/abilities.py` contains `class AbilityCategory`
    - `src/smdgf/schemas/abilities.py` contains `implicit_stance`
    - `src/smdgf/schemas/task.py` contains `class TaskDefinition`
    - `src/smdgf/schemas/spec.py` contains `class TaskSpecification`
  </acceptance_criteria>
  <done>Contract-layer type system exists for ability metadata, task definitions, and structured task specifications.</done>
</task>

<task type="auto">
  <name>Task 2: Implement canonical sample model and in-memory task registry</name>
  <files>src/smdgf/schemas/canonical.py, src/smdgf/registry.py, tests/unit/test_contract_models.py</files>
  <read_first>.planning/phases/01-canonical-contracts/01-RESEARCH.md, src/smdgf/schemas/task.py, src/smdgf/schemas/spec.py, pyproject.toml</read_first>
  <action>Create `CanonicalQuestion`, `CanonicalAnswer`, `ProvenanceRecord`, and `CanonicalSample` in `src/smdgf/schemas/canonical.py`. Ensure the canonical sample stores task id, structured scene payload or scene text, role state, latent state, canonical answer semantics, and provenance fields without exporter-specific distractor options. Implement `TaskRegistry` in `src/smdgf/registry.py` with methods to register, get, list, and clear `TaskDefinition` objects, raising a `ValueError` on duplicate `task_id`. Add `tests/unit/test_contract_models.py` with tests named `test_task_definition_validation`, `test_task_specification_supports_qc_expectations`, `test_canonical_sample_is_export_agnostic`, and `test_registry_rejects_duplicate_task_id`.</action>
  <verify>python - <<'PY'\nfrom pathlib import Path\nchecks = {\n  'src/smdgf/schemas/canonical.py': ['class CanonicalSample', 'class ProvenanceRecord'],\n  'src/smdgf/registry.py': ['class TaskRegistry', 'ValueError'],\n  'tests/unit/test_contract_models.py': ['def test_registry_rejects_duplicate_task_id', 'def test_canonical_sample_is_export_agnostic'],\n}\nfor path, needles in checks.items():\n    text = Path(path).read_text()\n    if any(n not in text for n in needles):\n        raise SystemExit(1)\nPY</verify>
  <acceptance_criteria>
    - `src/smdgf/schemas/canonical.py` contains `class CanonicalSample`
    - `src/smdgf/registry.py` contains `class TaskRegistry`
    - `tests/unit/test_contract_models.py` contains `def test_registry_rejects_duplicate_task_id`
    - `tests/unit/test_contract_models.py` contains `def test_canonical_sample_is_export_agnostic`
  </acceptance_criteria>
  <done>Canonical sample and registry implementation exist with concrete test coverage for duplicates and export-agnostic semantics.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
  <boundary>
    <name>Task specification input to schema validator</name>
    <description>Research-authored task specs and definitions enter the contract layer before generation.</description>
    <data_crossing>Task ids, capability metadata, latent variable declarations, QC expectations</data_crossing>
  </boundary>
</trust_boundaries>
<threat_register>
  <threat>
    <threat_id>T-1-03</threat_id>
    <category>Tampering</category>
    <component>contract models</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Use strict Pydantic models and enum-constrained fields so malformed or out-of-taxonomy definitions fail validation.</mitigation_plan>
    <files_to_check>src/smdgf/schemas/abilities.py, src/smdgf/schemas/task.py, src/smdgf/schemas/spec.py</files_to_check>
  </threat>
  <threat>
    <threat_id>T-1-04</threat_id>
    <category>Denial of Service</category>
    <component>task registry</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Reject duplicate `task_id` registration immediately instead of allowing silent overwrite or registry growth by collision.</mitigation_plan>
    <files_to_check>src/smdgf/registry.py, tests/unit/test_contract_models.py</files_to_check>
  </threat>
  <threat>
    <threat_id>T-1-05</threat_id>
    <category>Information Disclosure</category>
    <component>canonical sample model</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Keep exporter-specific distractors and presentation fields out of canonical sample contracts.</mitigation_plan>
    <files_to_check>src/smdgf/schemas/canonical.py, tests/unit/test_contract_models.py</files_to_check>
  </threat>
</threat_register>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/unit/test_contract_models.py -q` succeeds
- [ ] Ability taxonomy, task definitions, structured task specs, and canonical sample models all exist
- [ ] Registry rejects duplicate `task_id` values
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Contract models and registry are usable as the source of truth for later phases
</success_criteria>

<output>
After completion, create `.planning/phases/01-canonical-contracts/01-02-SUMMARY.md`
</output>
