---
phase: 03-generation-runtime
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/smdgf/generation/runtime.py
  - tests/unit/test_generation_runtime.py
autonomous: true
requirements: [GEN-01, GEN-03]
user_setup: []
must_haves:
  truths:
    - "Generation jobs can execute batches over normalized provider requests."
    - "Interrupted runs can resume without reprocessing completed item ids."
    - "Failures and retries are explicit runtime state, not hidden control flow."
  artifacts:
    - path: "src/smdgf/generation/runtime.py"
      provides: "Batch runner, retry policy, checkpoint persistence, and resume logic"
      min_lines: 70
      contains: "class GenerationRuntime"
    - path: "tests/unit/test_generation_runtime.py"
      provides: "Retry and resumability tests"
      min_lines: 35
      contains: "def test_runtime_resume_skips_completed_requests"
  key_links:
    - from: "src/smdgf/generation/runtime.py"
      to: "src/smdgf/generation/providers.py"
      via: "runtime executes normalized provider adapter calls"
      pattern: "GenerationProvider|LiteLLMGenerationProvider"
    - from: "src/smdgf/generation/runtime.py"
      to: "src/smdgf/generation/prompts.py"
      via: "runtime builds prompts before provider invocation"
      pattern: "build_generation_prompt"
---

<objective>
Implement the batch generation runtime with retry, checkpoint, and resume behavior.

Purpose: Provider adapters and prompt assembly are not enough unless generation jobs can be resumed safely and observed through explicit runtime state.
Output: runtime module plus unit tests for retries and resumability.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-generation-runtime/03-RESEARCH.md
@.planning/phases/03-generation-runtime/03-02-PLAN.md
@src/smdgf/generation/models.py
@src/smdgf/generation/providers.py
@src/smdgf/generation/prompts.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement resumable batch runtime and checkpoint helpers</name>
  <files>src/smdgf/generation/runtime.py</files>
  <read_first>.planning/phases/03-generation-runtime/03-RESEARCH.md, src/smdgf/generation/models.py, src/smdgf/generation/providers.py, src/smdgf/generation/prompts.py</read_first>
  <action>Create `src/smdgf/generation/runtime.py` with a `GenerationRuntime` that accepts normalized requests, applies prompt assembly, executes provider calls, records explicit item status, writes checkpoint state, and supports resume by skipping already completed request ids. Include configurable max retries and explicit failed-item state after retries are exhausted.</action>
  <verify>python3 - <<'PY'\nfrom pathlib import Path\ntext = Path('src/smdgf/generation/runtime.py').read_text()\nfor needle in ['class GenerationRuntime', 'max_retries', 'resume', 'checkpoint']:\n    if needle not in text:\n        raise SystemExit(1)\nPY</verify>
  <acceptance_criteria>
    - `src/smdgf/generation/runtime.py` contains `class GenerationRuntime`
    - `src/smdgf/generation/runtime.py` contains `max_retries`
    - `src/smdgf/generation/runtime.py` contains `resume`
  </acceptance_criteria>
  <done>Generation runtime can checkpoint and resume batch execution.</done>
</task>

<task type="auto">
  <name>Task 2: Add retry and resume tests for runtime behavior</name>
  <files>tests/unit/test_generation_runtime.py</files>
  <read_first>.planning/phases/03-generation-runtime/03-VALIDATION.md, src/smdgf/generation/runtime.py, src/smdgf/generation/providers.py, tests/conftest.py</read_first>
  <action>Create `tests/unit/test_generation_runtime.py` with tests named `test_runtime_resume_skips_completed_requests`, `test_runtime_retries_failed_requests_before_marking_failed`, and `test_runtime_records_explicit_item_status_transitions`. Use stub provider implementations and temporary checkpoint paths; do not call real providers.</action>
  <verify>python3 -m pytest tests/unit/test_generation_runtime.py -q</verify>
  <acceptance_criteria>
    - `tests/unit/test_generation_runtime.py` contains `def test_runtime_resume_skips_completed_requests`
    - `tests/unit/test_generation_runtime.py` contains `def test_runtime_retries_failed_requests_before_marking_failed`
  </acceptance_criteria>
  <done>Runtime behavior is safe for retries and resumable execution.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
  <boundary>
    <name>Generation run state to resumed execution</name>
    <description>Checkpointed run metadata governs whether requests are retried, skipped, or completed during resume.</description>
    <data_crossing>Request ids, checkpoint state, retry counters, item status, provider results</data_crossing>
  </boundary>
</trust_boundaries>
<threat_register>
  <threat>
    <threat_id>T-3-05</threat_id>
    <category>Repudiation</category>
    <component>resume logic</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Persist completed item ids and explicit status transitions so reruns skip already completed work instead of duplicating generation silently.</mitigation_plan>
    <files_to_check>src/smdgf/generation/runtime.py, tests/unit/test_generation_runtime.py</files_to_check>
  </threat>
  <threat>
    <threat_id>T-3-06</threat_id>
    <category>Denial of Service</category>
    <component>retry loop</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Bound retries with explicit max retry count and final failed state rather than unbounded re-execution.</mitigation_plan>
    <files_to_check>src/smdgf/generation/runtime.py, tests/unit/test_generation_runtime.py</files_to_check>
  </threat>
</threat_register>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `python3 -m pytest tests/unit/test_generation_runtime.py -q` succeeds
- [ ] Runtime resume skips already completed request ids
- [ ] Retry logic is bounded and failure state is explicit
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Runtime batches can execute, checkpoint, and resume safely
</success_criteria>

<output>
After completion, create `.planning/phases/03-generation-runtime/03-03-SUMMARY.md`
</output>
