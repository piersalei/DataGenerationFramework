---
phase: 03-generation-runtime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/smdgf/generation/__init__.py
  - src/smdgf/generation/models.py
  - src/smdgf/generation/providers.py
  - tests/unit/test_generation_provider.py
autonomous: true
requirements: [GEN-01, GEN-04]
user_setup: []
must_haves:
  truths:
    - "Researchers can configure one provider surface for local and remote LLM backends."
    - "Provider responses are normalized into framework-owned request/result contracts."
    - "Phase 3 does not leak LiteLLM-specific request shapes into core runtime contracts."
  artifacts:
    - path: "src/smdgf/generation/models.py"
      provides: "ProviderConfig, GenerationRequest, GenerationResult, and run manifest contracts"
      min_lines: 70
      contains: "class GenerationRequest"
    - path: "src/smdgf/generation/providers.py"
      provides: "GenerationProvider protocol and LiteLLM-backed provider implementation"
      min_lines: 60
      contains: "class LiteLLMGenerationProvider"
    - path: "tests/unit/test_generation_provider.py"
      provides: "Provider normalization and provenance response tests"
      min_lines: 30
      contains: "def test_provider_config_supports_local_and_remote_backends"
  key_links:
    - from: "src/smdgf/generation/providers.py"
      to: "src/smdgf/generation/models.py"
      via: "provider adapter consumes framework-owned request/result contracts"
      pattern: "GenerationRequest|GenerationResult|ProviderConfig"
    - from: "tests/unit/test_generation_provider.py"
      to: "src/smdgf/generation/providers.py"
      via: "provider adapter tests validate normalized backend behavior"
      pattern: "LiteLLMGenerationProvider|ProviderConfig"
---

<objective>
Implement the provider-facing contracts and the first provider adapter for the generation runtime.

Purpose: Phase 3 needs one normalized execution boundary before prompt assembly and resumable runs can be built safely.
Output: generation contract models, LiteLLM-backed provider adapter, and provider-focused unit tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-generation-runtime/03-RESEARCH.md
@.planning/phases/02-scenario-and-sampling-engine/02-VERIFICATION.md
@src/smdgf/schemas/task.py
@src/smdgf/schemas/canonical.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add generation runtime contracts and package exports</name>
  <files>pyproject.toml, src/smdgf/generation/__init__.py, src/smdgf/generation/models.py</files>
  <read_first>.planning/phases/03-generation-runtime/03-RESEARCH.md, .planning/REQUIREMENTS.md, src/smdgf/schemas/task.py, src/smdgf/schemas/canonical.py</read_first>
  <action>Create `src/smdgf/generation/__init__.py` and `src/smdgf/generation/models.py`. Add strict Pydantic models for `ProviderConfig`, `GenerationRequest`, `GenerationResult`, `GenerationUsage`, `GenerationError`, `GenerationRunItem`, and `GenerationRunManifest`. Ensure `GenerationRequest` carries exact fields `request_id`, `task_id`, `scenario_sample`, `prompt_text`, `provider`, `model`, and `seed`. Update `pyproject.toml` to include the v1 runtime dependency needed for the default provider adapter. Export generation contract symbols from `src/smdgf/generation/__init__.py`.</action>
  <verify>python3 - <<'PY'\nfrom pathlib import Path\nchecks = {\n  'src/smdgf/generation/models.py': ['class ProviderConfig', 'class GenerationRequest', 'class GenerationResult', 'class GenerationRunManifest'],\n  'src/smdgf/generation/__init__.py': ['GenerationRequest', 'GenerationResult', 'ProviderConfig'],\n  'pyproject.toml': ['litellm'],\n}\nfor path, needles in checks.items():\n    text = Path(path).read_text()\n    if any(n not in text for n in needles):\n        raise SystemExit(1)\nPY</verify>
  <acceptance_criteria>
    - `src/smdgf/generation/models.py` contains `class GenerationRequest`
    - `src/smdgf/generation/models.py` contains `class GenerationRunManifest`
    - `pyproject.toml` contains `litellm`
  </acceptance_criteria>
  <done>Generation runtime contracts exist and are exportable for later runtime layers.</done>
</task>

<task type="auto">
  <name>Task 2: Implement LiteLLM-backed provider adapter and normalization tests</name>
  <files>src/smdgf/generation/providers.py, tests/unit/test_generation_provider.py</files>
  <read_first>.planning/phases/03-generation-runtime/03-VALIDATION.md, src/smdgf/generation/models.py, tests/conftest.py</read_first>
  <action>Create `src/smdgf/generation/providers.py` with a `GenerationProvider` protocol and `LiteLLMGenerationProvider` implementation that converts `GenerationRequest` into a normalized completion call surface and converts provider output into `GenerationResult`. Add `tests/unit/test_generation_provider.py` with tests named `test_provider_config_supports_local_and_remote_backends`, `test_generation_result_captures_provider_provenance`, and `test_litellm_provider_normalizes_error_payloads`. Use stubbed provider call points instead of real network calls.</action>
  <verify>python3 -m pytest tests/unit/test_generation_provider.py -q</verify>
  <acceptance_criteria>
    - `src/smdgf/generation/providers.py` contains `class LiteLLMGenerationProvider`
    - `tests/unit/test_generation_provider.py` contains `def test_provider_config_supports_local_and_remote_backends`
    - `tests/unit/test_generation_provider.py` contains `def test_generation_result_captures_provider_provenance`
  </acceptance_criteria>
  <done>Provider execution now flows through one framework-owned adapter surface.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
  <boundary>
    <name>Generation runtime to external model backends</name>
    <description>Prompted generation requests cross from framework-owned contracts into local or remote provider APIs.</description>
    <data_crossing>Prompt text, provider configuration, model ids, response payloads, error state</data_crossing>
  </boundary>
</trust_boundaries>
<threat_register>
  <threat>
    <threat_id>T-3-01</threat_id>
    <category>Tampering</category>
    <component>provider adapter config</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Normalize provider configuration in strict models so local and remote backends enter execution through the same validated surface.</mitigation_plan>
    <files_to_check>src/smdgf/generation/models.py, src/smdgf/generation/providers.py, tests/unit/test_generation_provider.py</files_to_check>
  </threat>
  <threat>
    <threat_id>T-3-02</threat_id>
    <category>Repudiation</category>
    <component>provider response normalization</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Convert provider responses and failures into framework-owned `GenerationResult` records that preserve provider/model/usage/error metadata explicitly.</mitigation_plan>
    <files_to_check>src/smdgf/generation/providers.py, tests/unit/test_generation_provider.py</files_to_check>
  </threat>
</threat_register>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `python3 -m pytest tests/unit/test_generation_provider.py -q` succeeds
- [ ] Provider adapter supports both local and remote backend config shapes
- [ ] Normalized generation results preserve provenance fields explicitly
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Provider execution is normalized behind framework-owned contracts
</success_criteria>

<output>
After completion, create `.planning/phases/03-generation-runtime/03-01-SUMMARY.md`
</output>
