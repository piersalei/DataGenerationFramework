---
phase: 03-generation-runtime
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - src/smdgf/generation/models.py
  - src/smdgf/generation/runtime.py
  - tests/unit/test_generation_manifest.py
autonomous: true
requirements: [GEN-03, GEN-04]
user_setup: []
must_haves:
  truths:
    - "Every generated candidate records prompt, provider, model, seed, and response provenance."
    - "Run manifests are structured artifacts, not implicit folder conventions."
    - "Manifest-backed reruns can identify already completed work deterministically."
  artifacts:
    - path: "src/smdgf/generation/models.py"
      provides: "Run manifest and run item provenance fields"
      min_lines: 90
      contains: "class GenerationRunManifest"
    - path: "tests/unit/test_generation_manifest.py"
      provides: "Manifest persistence and rerun behavior tests"
      min_lines: 35
      contains: "def test_manifest_records_prompt_seed_provider_and_response"
  key_links:
    - from: "src/smdgf/generation/runtime.py"
      to: "src/smdgf/generation/models.py"
      via: "runtime persists structured run manifest records"
      pattern: "GenerationRunManifest|GenerationRunItem"
    - from: "tests/unit/test_generation_manifest.py"
      to: "src/smdgf/generation/runtime.py"
      via: "manifest tests verify runtime persistence and replay behavior"
      pattern: "GenerationRuntime|GenerationRunManifest"
---

<objective>
Persist provenance-rich run manifests and verify manifest-backed replay behavior.

Purpose: Phase 3 should end with durable runtime artifacts that Phase 4 QC and Phase 6 reproducibility can inspect directly.
Output: provenance-complete manifest persistence and fixture-backed manifest tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-generation-runtime/03-RESEARCH.md
@.planning/phases/03-generation-runtime/03-03-PLAN.md
@src/smdgf/generation/models.py
@src/smdgf/generation/runtime.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persist structured manifest provenance from runtime execution</name>
  <files>src/smdgf/generation/models.py, src/smdgf/generation/runtime.py</files>
  <read_first>.planning/phases/03-generation-runtime/03-RESEARCH.md, src/smdgf/generation/models.py, src/smdgf/generation/runtime.py</read_first>
  <action>Extend the runtime models and runtime persistence so `GenerationRunManifest` and `GenerationRunItem` store prompt fingerprint, prompt text snapshot or reference, provider id, model id, seed, response text, normalized usage, timestamps, and final item status. Ensure manifest serialization is deterministic and can be written/read without custom code execution.</action>
  <verify>python3 - <<'PY'\nfrom pathlib import Path\ntext = Path('src/smdgf/generation/models.py').read_text()\nfor needle in ['prompt_fingerprint', 'response_text', 'status', 'seed']:\n    if needle not in text:\n        raise SystemExit(1)\nPY</verify>
  <acceptance_criteria>
    - `src/smdgf/generation/models.py` contains `prompt_fingerprint`
    - `src/smdgf/generation/models.py` contains `response_text`
    - `src/smdgf/generation/models.py` contains `status`
  </acceptance_criteria>
  <done>Manifest models retain the provenance Phase 4 and Phase 6 require.</done>
</task>

<task type="auto">
  <name>Task 2: Add manifest persistence and replay tests</name>
  <files>tests/unit/test_generation_manifest.py</files>
  <read_first>.planning/phases/03-generation-runtime/03-VALIDATION.md, src/smdgf/generation/models.py, src/smdgf/generation/runtime.py, tests/conftest.py</read_first>
  <action>Create `tests/unit/test_generation_manifest.py` with tests named `test_manifest_records_prompt_seed_provider_and_response`, `test_manifest_round_trip_preserves_item_status`, and `test_manifest_backed_rerun_skips_completed_items`. Use temporary files and stubbed runtime/provider behavior only.</action>
  <verify>python3 -m pytest tests/unit/test_generation_manifest.py -q</verify>
  <acceptance_criteria>
    - `tests/unit/test_generation_manifest.py` contains `def test_manifest_records_prompt_seed_provider_and_response`
    - `tests/unit/test_generation_manifest.py` contains `def test_manifest_backed_rerun_skips_completed_items`
  </acceptance_criteria>
  <done>Runtime artifacts are durable, provenance-rich, and replay-safe.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
  <boundary>
    <name>Runtime execution to persisted provenance artifacts</name>
    <description>Provider requests and responses are converted into manifest records that future phases trust for QC and replay.</description>
    <data_crossing>Prompt metadata, provider ids, model ids, seeds, response text, status, timestamps</data_crossing>
  </boundary>
</trust_boundaries>
<threat_register>
  <threat>
    <threat_id>T-3-07</threat_id>
    <category>Repudiation</category>
    <component>manifest provenance</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Persist prompt, provider, model, seed, usage, and response details in structured manifest items so outputs can be audited without replaying providers.</mitigation_plan>
    <files_to_check>src/smdgf/generation/models.py, src/smdgf/generation/runtime.py, tests/unit/test_generation_manifest.py</files_to_check>
  </threat>
  <threat>
    <threat_id>T-3-08</threat_id>
    <category>Repudiation</category>
    <component>manifest-backed reruns</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Make reruns consult manifest item status and request ids deterministically so completed work is never replayed silently.</mitigation_plan>
    <files_to_check>src/smdgf/generation/runtime.py, tests/unit/test_generation_manifest.py</files_to_check>
  </threat>
</threat_register>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `python3 -m pytest tests/unit/test_generation_manifest.py -q` succeeds
- [ ] Manifests persist prompt, provider, model, seed, and response provenance explicitly
- [ ] Manifest-backed reruns skip completed item ids deterministically
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 3 ends with provenance-rich, replay-safe runtime artifacts
</success_criteria>

<output>
After completion, create `.planning/phases/03-generation-runtime/03-04-SUMMARY.md`
</output>
