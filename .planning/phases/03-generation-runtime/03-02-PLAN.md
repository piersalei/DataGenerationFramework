---
phase: 03-generation-runtime
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/smdgf/generation/prompts.py
  - tests/unit/test_prompt_assembly.py
autonomous: true
requirements: [GEN-02, GEN-04]
user_setup: []
must_haves:
  truths:
    - "Prompt assembly is deterministic from task contracts and sampled scenario state."
    - "Prompt metadata captures task, scenario, provider-facing seed context, and a stable fingerprint."
    - "Prompt rendering does not embed ad hoc provider logic."
  artifacts:
    - path: "src/smdgf/generation/prompts.py"
      provides: "Prompt assembly functions and prompt metadata model"
      min_lines: 60
      contains: "def build_generation_prompt"
    - path: "tests/unit/test_prompt_assembly.py"
      provides: "Prompt determinism and metadata tests"
      min_lines: 30
      contains: "def test_prompt_assembly_is_deterministic"
  key_links:
    - from: "src/smdgf/generation/prompts.py"
      to: "src/smdgf/schemas/task.py"
      via: "prompt assembly uses task definition and spec metadata"
      pattern: "TaskDefinition|TaskSpecification"
    - from: "src/smdgf/generation/prompts.py"
      to: "src/smdgf/schemas/scene.py"
      via: "prompt assembly consumes ScenarioSample state"
      pattern: "ScenarioSample"
---

<objective>
Implement deterministic prompt assembly from task contracts and sampled scenario state.

Purpose: The generation runtime needs reproducible prompt text and metadata before batch execution can be resumed or audited.
Output: prompt assembly module plus prompt-determinism unit tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-generation-runtime/03-RESEARCH.md
@.planning/phases/03-generation-runtime/03-01-PLAN.md
@src/smdgf/generation/models.py
@src/smdgf/schemas/task.py
@src/smdgf/schemas/spec.py
@src/smdgf/schemas/scene.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement deterministic prompt assembly helpers</name>
  <files>src/smdgf/generation/prompts.py</files>
  <read_first>.planning/phases/03-generation-runtime/03-RESEARCH.md, src/smdgf/generation/models.py, src/smdgf/schemas/task.py, src/smdgf/schemas/spec.py, src/smdgf/schemas/scene.py</read_first>
  <action>Create `src/smdgf/generation/prompts.py` with a function `build_generation_prompt(...)` that accepts task contract inputs plus `ScenarioSample` and returns prompt text plus prompt metadata. Include a stable prompt fingerprint derived from normalized prompt inputs, and ensure slot, role, relation, and latent-state rendering iterates deterministically. Keep provider-specific execution concerns out of this module.</action>
  <verify>python3 - <<'PY'\nfrom pathlib import Path\ntext = Path('src/smdgf/generation/prompts.py').read_text()\nfor needle in ['def build_generation_prompt', 'prompt_fingerprint', 'ScenarioSample']:\n    if needle not in text:\n        raise SystemExit(1)\nPY</verify>
  <acceptance_criteria>
    - `src/smdgf/generation/prompts.py` contains `def build_generation_prompt`
    - `src/smdgf/generation/prompts.py` contains `prompt_fingerprint`
  </acceptance_criteria>
  <done>Deterministic prompt assembly exists and emits prompt metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Add prompt determinism and metadata tests</name>
  <files>tests/unit/test_prompt_assembly.py</files>
  <read_first>.planning/phases/03-generation-runtime/03-VALIDATION.md, src/smdgf/generation/prompts.py, src/smdgf/schemas/task.py, src/smdgf/schemas/spec.py, src/smdgf/schemas/scene.py, tests/conftest.py</read_first>
  <action>Create `tests/unit/test_prompt_assembly.py` with tests named `test_prompt_assembly_is_deterministic`, `test_prompt_metadata_contains_task_scenario_and_seed_context`, and `test_prompt_includes_roles_relations_and_latent_state_context`. Assert on concrete prompt metadata and prompt fragments rather than only checking non-empty strings.</action>
  <verify>python3 -m pytest tests/unit/test_prompt_assembly.py -q</verify>
  <acceptance_criteria>
    - `tests/unit/test_prompt_assembly.py` contains `def test_prompt_assembly_is_deterministic`
    - `tests/unit/test_prompt_assembly.py` contains `def test_prompt_metadata_contains_task_scenario_and_seed_context`
  </acceptance_criteria>
  <done>Prompt assembly behavior is reproducible and auditable.</done>
</task>

</tasks>

<threat_model>
<trust_boundaries>
  <boundary>
    <name>Task and scenario contracts to prompt text</name>
    <description>Structured task and sampled scenario state cross into a provider-facing prompt representation.</description>
    <data_crossing>Task metadata, question patterns, sampled slots, role graph, latent states, seed context</data_crossing>
  </boundary>
</trust_boundaries>
<threat_register>
  <threat>
    <threat_id>T-3-03</threat_id>
    <category>Tampering</category>
    <component>prompt assembly</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Use deterministic ordering and stable fingerprints so prompt content changes only when upstream contracts or sampled state change.</mitigation_plan>
    <files_to_check>src/smdgf/generation/prompts.py, tests/unit/test_prompt_assembly.py</files_to_check>
  </threat>
  <threat>
    <threat_id>T-3-04</threat_id>
    <category>Repudiation</category>
    <component>prompt metadata</component>
    <disposition>mitigate</disposition>
    <mitigation_plan>Persist prompt metadata with task id, scenario id, seed, and fingerprint so generated outputs can be replayed and audited.</mitigation_plan>
    <files_to_check>src/smdgf/generation/prompts.py, tests/unit/test_prompt_assembly.py</files_to_check>
  </threat>
</threat_register>
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `python3 -m pytest tests/unit/test_prompt_assembly.py -q` succeeds
- [ ] Prompt assembly is deterministic for equivalent task and scenario inputs
- [ ] Prompt metadata captures prompt fingerprint and replay context explicitly
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Prompt assembly is ready for batch generation execution
</success_criteria>

<output>
After completion, create `.planning/phases/03-generation-runtime/03-02-SUMMARY.md`
</output>
